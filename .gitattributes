* text=auto eol=lf
# LSP cli-case stdin files carry framed protocol bytes (\r\n headers) — never
# normalize. expected_stdout gets the same treatment: a recorded golden for an
# LSP case embeds the server's framed responses, CRLF included.
tests/cli-cases/*/stdin -text
tests/cli-cases/*/expected_stdout -text
