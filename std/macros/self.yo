// QUESTION: Should we support below? Like ruby and coffeescript.
// Instance variable
/*
@ :: ((quote(field): Expr) -> unquote(Expr))
  quote (self.(unquote(field)))
;

// Class variable
@@ :: ((quote(field): Expr) -> unquote(Expr))
  quote (Self.(unquote(field)))
;
*/