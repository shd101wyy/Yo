

if :: ((quote(condition) : Expr,
        quote(then) : Expr,
        quote(else) : Expr) -> unquote(Expr))
  quote
    cond 
      unquote(condition) => unquote(then),
      true => unquote(else)
;

export 
  if;