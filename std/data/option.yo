Option :: ((@(T): Type)-> @(Type))
  enum
    Some(value: T),
    None
;

// Alias of the Option type
? :: Option;

// Nullable pointer
?* :: ((@(T): Type) -> @(Type))
  ?(*(T))
;
// Nullable mutable pointer
?*! :: ((@(T): Type) -> @(Type))
  ?(*!(T))
;

export 
  Option,
  ?,
  ?*,
  ?*!
;