{ Drop } :: import "../../modules/common.yo";


DropArray :: (fn(
  forall(T : Type, S : usize), 
  using(DropT : Drop(T)))
  -> compt(Drop(Array(T, S))))
  Drop(Array(T, S))
    drop : 
      (self)-> 
        for self, elem => {
          DropT.drop(elem);
        };
  ;

export DropArray;