{ Drop } :: import "../../modules/common.yo";


DropArray :: (fn(
  forall(T : Type, S : usize), 
  using(DropT) : (T <: Drop) )
  -> compt(Drop))
  Drop
    Self : Array(T, S),
    drop : 
      (self)-> 
        for self, elem => {
          DropT.drop(elem);
        };
  ;

export DropArray;