open import "../../builtins.yo";
open import "../../modules/arithmetic.yo";
open import "../../modules/logic.yo";
{ Eq } :: import "../../modules/eq.yo";
{ Ord } :: import "../../modules/ord.yo";
{ Clone, Drop } :: import "../../modules/common.yo";

/**
 * arithmetic
 */
LongAdd :: Add(long, long)
  (+): ((a, b) -> __yo_op_add(a, b))
;

LongSub :: Sub(long, long)
  (-): ((a, b) -> __yo_op_sub(a, b))
;

LongMul :: Mul(long, long)
  (*): ((a, b) -> __yo_op_mul(a, b))
;

LongDiv :: Div(long, long)
  (/): ((a, b) -> __yo_op_div(a, b))
;

LongMod :: Mod(long, long)
  (%): ((a, b) -> __yo_op_mod(a, b))
;

LongNegate :: Negate(long)
  (neg): ((a) -> __yo_op_neg(a))
;

/**
 * logic
 */
LongLogicalNot :: LogicalNot(long)
  (!): ((a) -> __yo_op_not(a))
;

LongBitNot :: BitNot(long)
  (~): ((a) -> __yo_op_bit_complement(a))
;

LongBitAnd :: BitAnd(long, long)
  (&): ((a, b) -> __yo_op_bit_and(a, b))
;

LongBitOr :: BitOr(long, long)
  (|): ((a, b) -> __yo_op_bit_or(a, b))
;

LongBitXor :: BitXor(long, long)
  (^): ((a, b) -> __yo_op_bit_xor(a, b))
;


/**
 * eq
 */
LongEq :: Eq(long, long)
  (==) : ((a, b) -> __yo_op_eq(a, b)),
  (!=) : ((a, b) -> __yo_op_neq(a, b))
;

/**
 * ord
 */
LongOrd :: Ord(long, long, using(LongEq))
  (<) : ((a, b) -> __yo_op_lt(a, b)),
  (<=) : ((a, b) -> __yo_op_lte(a, b)),
  (>) : ((a, b) -> __yo_op_gt(a, b)),
  (>=) : ((a, b) -> __yo_op_gte(a, b))
;

LongDrop :: Drop(long)
  drop: ((self) ->
    // No-op for long, as it does not require any special cleanup
    __yo_noop()
  )
;

LongClone :: Clone(long)
  clone: ((self) ->
    // No-op for long, as it is a primitive type
    __yo_return_self(self.*)
  )
;

LongMethods :: impl {
  Self :: long;

  as :: (fn(self: Self, compt(Target) : Type) -> Target)
    __yo_as(self, Target)
  ;

  export
    Self,
    as
  ;
};

export
  LongAdd,
  LongSub,
  LongMul,
  LongDiv,
  LongMod,
  LongNegate,
  LongLogicalNot,
  LongBitNot,
  LongBitAnd,
  LongBitOr,
  LongBitXor,
  LongEq,
  LongOrd,
  LongDrop,
  LongClone,
  LongMethods
;