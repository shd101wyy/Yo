extern "yo",
  yo_get_thread_id :
    fn() -> usize
;

ThreadMethods :: impl {
  (wait) :: (fn(forall(T: Type), thread: Thread(T)) -> T)
    __yo_thread_wait(thread)
  ;

  export
    wait
  ;
};

thread_id :: yo_get_thread_id;

export
  ThreadMethods,
  thread_id
;