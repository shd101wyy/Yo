open import "../../builtins.yo";
open import "../../modules/arithmetic.yo";
open import "../../modules/logic.yo";
{ Eq } :: import "../../modules/eq.yo";
{ Ord } :: import "../../modules/ord.yo";
{ Hash } :: import "../../modules/hash.yo";
{ Clone, Drop } :: import "../../modules/common.yo";

/**
 * arithmetic
 */
I64Add :: Add(i64, i64)
  (+): ((a, b) -> __yo_op_add(a, b))
;

I64Sub :: Sub(i64, i64)
  (-): ((a, b) -> __yo_op_sub(a, b))
;

I64Mul :: Mul(i64, i64)
  (*): ((a, b) -> __yo_op_mul(a, b))
;

I64Div :: Div(i64, i64)
  (/): ((a, b) -> __yo_op_div(a, b))
;

I64Mod :: Mod(i64, i64)
  (%): ((a, b) -> __yo_op_mod(a, b))
;

I64Negate :: Negate(i64)
  (neg): ((a) -> __yo_op_neg(a))
;

I64BitLeftShift :: BitLeftShift(i64, i64)
  (<<): ((a, b) -> __yo_op_bit_left_shift(a, b))
;

I64BitRightShift :: BitRightShift(i64, i64)
  (>>): ((a, b) -> __yo_op_bit_right_shift(a, b))
;

/**
 * logic
 */
I64LogicalNot :: LogicalNot(i64)
  (!): ((a) -> __yo_op_not(a))
;

I64BitNot :: BitNot(i64)
  (~): ((a) -> __yo_op_bit_complement(a))
;

I64BitAnd :: BitAnd(i64, i64)
  (&): ((a, b) -> __yo_op_bit_and(a, b))
;

I64BitOr :: BitOr(i64, i64)
  (|): ((a, b) -> __yo_op_bit_or(a, b))
;

I64BitXor :: BitXor(i64, i64)
  (^): ((a, b) -> __yo_op_bit_xor(a, b))
;


/**
 * eq
 */
I64Eq :: Eq(i64, i64)
  (==) : ((a, b) -> __yo_op_eq(a, b)),
  (!=) : ((a, b) -> __yo_op_neq(a, b))
;

/**
 * ord
 */
I64Ord :: Ord(i64, i64, using(I64Eq))
  (<) : ((a, b) -> __yo_op_lt(a, b)),
  (<=) : ((a, b) -> __yo_op_lte(a, b)),
  (>) : ((a, b) -> __yo_op_gt(a, b)),
  (>=) : ((a, b) -> __yo_op_gte(a, b))
;

/**
 * hash
 */
I64Hash :: Hash(i64)
  (hash): ((value) -> __yo_as(value, u64))
;

I64Drop :: Drop(i64)
  drop: ((self) ->
    // No-op for i64, as it does not require any special cleanup
    __yo_noop()
  )
;

I64Clone :: Clone(i64)
  clone: ((self) ->
    // No-op for i64, as it is a primitive type
    __yo_return_self(self.*)
  )
;

I64Methods :: impl {
  Self :: i64;

  (as) :: (fn(self: Self, compt(Target): Type) -> Target)
    __yo_as(self, Target)
  ;
  
  export Self,
    as
  ;
};

I64ComptMethods :: impl {
  Self :: i64;

  (+) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_i64_add(lhs, rhs)
  ;

  (-) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_i64_sub(lhs, rhs)
  ;

  (*) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_i64_mul(lhs, rhs)
  ;

  (/) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_i64_div(lhs, rhs)
  ;

  (%) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_i64_mod(lhs, rhs)
  ;

  (==) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_i64_eq(lhs, rhs)
  ;

  (!=) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_i64_neq(lhs, rhs)
  ;

  (<) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_i64_lt(lhs, rhs)
  ;

  (<=) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_i64_lte(lhs, rhs)
  ;

  (>) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_i64_gt(lhs, rhs)
  ;

  (>=) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_i64_gte(lhs, rhs)
  ;

  neg :: (fn(compt(self): Self) -> compt(Self))
    __yo_i64_neg(self)
  ;

  (as) :: (fn(compt(self): Self, compt(Target): Type) -> compt(Target))
    __yo_i64_as(self, Target)
  ;

  (to_string) :: (fn(compt(self): Self) -> compt(compt_string))
    __yo_i64_to_string(self)
  ;

  export
    Self,
    +,
    -,
    *,
    /,
    %,
    ==,
    !=,
    <,
    <=,
    >,
    >=,
    neg,
    as,
    to_string
  ;
};

export
  I64Add,
  I64Sub,
  I64Mul,
  I64Div,
  I64Mod,
  I64BitLeftShift,
  I64BitRightShift,
  I64Negate,
  I64LogicalNot,
  I64BitNot,
  I64BitAnd,
  I64BitOr,
  I64BitXor,
  I64Eq,
  I64Ord,
  I64Hash,
  I64Drop,
  I64Clone,
  I64Methods,
  I64ComptMethods
;
