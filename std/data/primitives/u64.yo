open import "../../builtins.yo";
open import "../../modules/arithmetic.yo";
open import "../../modules/logic.yo";
{ Eq } :: import "../../modules/eq.yo";
{ Ord } :: import "../../modules/ord.yo";
{ Hash } :: import "../../modules/hash.yo";
{ Clone, Drop } :: import "../../modules/common.yo";

U64 :: impl {
  Self :: u64;
  export Self;

  // === Arithmetic ===
  _Add :: Add(u64, u64)
    (+): ((a, b) -> __yo_op_add(a, b))
  ;
  _Sub :: Sub(u64, u64)
    (-): ((a, b) -> __yo_op_sub(a, b))
  ;
  _Mul :: Mul(u64, u64)
    (*): ((a, b) -> __yo_op_mul(a, b))
  ;
  _Div :: Div(u64, u64)
    (/): ((a, b) -> __yo_op_div(a, b))
  ;
  _Mod :: Mod(u64, u64)
    (%): ((a, b) -> __yo_op_mod(a, b))
  ;
  _Negate :: Negate(u64)
    (neg): ((a) -> __yo_op_neg(a))
  ;
  _BitLeftShift :: BitLeftShift(u64, u64)
    (<<): ((a, b) -> __yo_op_bit_left_shift(a, b))
  ;
  _BitRightShift :: BitRightShift(u64, u64)
    (>>): ((a, b) -> __yo_op_bit_right_shift(a, b))
  ;
  export
    Add : _Add,
    Sub : _Sub,
    Mul : _Mul,
    Div : _Div,
    Mod : _Mod,
    Negate : _Negate,
    BitLeftShift : _BitLeftShift,
    BitRightShift : _BitRightShift
  ;

  // === Logic ===
  _LogicalNot :: LogicalNot(u64)
    (!): ((a) -> __yo_op_not(a))
  ;
  _BitNot :: BitNot(u64)
    (~): ((a) -> __yo_op_bit_complement(a))
  ;
  _BitAnd :: BitAnd(u64, u64)
    (&): ((a, b) -> __yo_op_bit_and(a, b))
  ;
  _BitOr :: BitOr(u64, u64)
    (|): ((a, b) -> __yo_op_bit_or(a, b))
  ;
  _BitXor :: BitXor(u64, u64)
    (^): ((a, b) -> __yo_op_bit_xor(a, b))
  ;
  export
    LogicalNot : _LogicalNot,
    BitNot : _BitNot,
    BitAnd : _BitAnd,
    BitOr : _BitOr,
    BitXor : _BitXor
  ;

  // === Eq ===
  _Eq :: Eq(u64, u64)
    (==) : ((a, b) -> __yo_op_eq(a, b)),
    (!=) : ((a, b) -> __yo_op_neq(a, b))
  ;
  export Eq : _Eq;

  // === Ord ===
  _Ord :: Ord(u64, u64, using(_Eq))
    (<) : ((a, b) -> __yo_op_lt(a, b)),
    (<=) : ((a, b) -> __yo_op_lte(a, b)),
    (>) : ((a, b) -> __yo_op_gt(a, b)),
    (>=) : ((a, b) -> __yo_op_gte(a, b))
  ;
  export Ord : _Ord;

  // === Hash ===
  _Hash :: Hash(u64)
    (hash): ((value) -> value)
  ;
  export Hash : _Hash;

  // === Drop ===
  _Drop :: Drop(u64)
    drop: ((self) ->
      // No-op for u64, as it does not require any special cleanup
      __yo_noop()
    )
  ;
  export Drop : _Drop;

  // === Clone ===
  _Clone :: Clone(u64)
    clone: ((self) ->
      // No-op for u64, as it is a primitive type
      __yo_return_self(self.*)
    )
  ;
  export Clone : _Clone;

  // === Other methods ===
  (as) :: (fn(self: Self, compt(Target): Type) -> Target)
    __yo_as(self, Target)
  ;
  export as;

};


ComptU64 :: impl {
  Self :: u64;

  (+) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_u64_add(lhs, rhs)
  ;

  (-) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_u64_sub(lhs, rhs)
  ;

  (*) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_u64_mul(lhs, rhs)
  ;

  (/) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_u64_div(lhs, rhs)
  ;

  (%) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(Self))
    __yo_u64_mod(lhs, rhs)
  ;

  (==) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_u64_eq(lhs, rhs)
  ;

  (!=) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_u64_neq(lhs, rhs)
  ;

  (<) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_u64_lt(lhs, rhs)
  ;

  (<=) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_u64_lte(lhs, rhs)
  ;

  (>) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_u64_gt(lhs, rhs)
  ;

  (>=) :: (fn(compt(lhs): Self, compt(rhs): Self) -> compt(boolean))
    __yo_u64_gte(lhs, rhs)
  ;

  (as) :: (fn(compt(self): Self, compt(Target): Type) -> compt(Target))
    __yo_u64_as(self, Target)
  ;

  (to_string) :: (fn(compt(self): Self) -> compt(compt_string))
    __yo_u64_to_string(self)
  ;

  export
    Self,
    +,
    -,
    *,
    /,
    %,
    ==,
    !=,
    <,
    <=,
    >,
    >=,
    as,
    to_string
  ;
};

export
  U64,
  ComptU64;

