open import "../../builtins.yo";
open import "../../modules/arithmetic.yo";
open import "../../modules/logic.yo";
{ Eq } :: import "../../modules/eq.yo";
{ Ord } :: import "../../modules/ord.yo";
{ Clone, Drop } :: import "../../modules/common.yo";

/**
 * arithmetic
 */
given(IntAdd) :: Add(int, int)
  (+): ((a, b) -> __yo_op_add(a, b))
;

given(IntSub) :: Sub(int, int)
  (-): ((a, b) -> __yo_op_sub(a, b))
;

given(IntMul) :: Mul(int, int)
  (*): ((a, b) -> __yo_op_mul(a, b))
;

given(IntDiv) :: Div(int, int)
  (/): ((a, b) -> __yo_op_div(a, b))
;

given(IntMod) :: Mod(int, int)
  (%): ((a, b) -> __yo_op_mod(a, b))
;

given(IntNegate) :: Negate(int)
  (neg): ((a) -> __yo_op_neg(a))
;

/**
 * logic
 */
given(IntLogicalNot) :: LogicalNot(int)
  (!): ((a) -> __yo_op_not(a))
;


given(IntBitNot) :: BitNot(int)
  (~): ((a) -> __yo_op_bit_complement(a))
;

given(IntBitAnd) :: BitAnd(int, int)
  (&): ((a, b) -> __yo_op_bit_and(a, b))
;

given(IntBitOr) :: BitOr(int, int)
  (|): ((a, b) -> __yo_op_bit_or(a, b))
;

given(IntBitXor) :: BitXor(int, int)
  (^): ((a, b) -> __yo_op_bit_xor(a, b))
;


/**
 * eq
 */
given(IntEq) :: Eq(int, int)
  (==) : ((a, b) -> __yo_op_eq(a, b)),
  (!=) : ((a, b) -> __yo_op_neq(a, b))
;

/**
 * ord
 */
given(IntOrd) :: Ord(int, int, using(IntEq))
  (<) : ((a, b) -> __yo_op_lt(a, b)),
  (<=) : ((a, b) -> __yo_op_lte(a, b)),
  (>) : ((a, b) -> __yo_op_gt(a, b)),
  (>=) : ((a, b) -> __yo_op_gte(a, b))
;

given(IntDrop) :: Drop(int)
  drop: ((self) ->
    // No-op for int, as it does not require any special cleanup
    __yo_noop()
  )
;

given(IntClone) :: Clone(int)
  clone: ((self) ->
    // No-op for int, as it is a primitive type
    __yo_return_self(self.*)
  )
;

given(IntMethods) :: impl {
  Self :: int;

  as :: (fn(self: Self, compt(Target) : Type) -> Target)
    __yo_as(self, Target)
  ;

  export
    Self,
    as
  ;
};

export
  IntAdd,
  IntSub,
  IntMul,
  IntDiv,
  IntMod,
  IntNegate,
  IntLogicalNot,
  IntBitNot,
  IntBitAnd,
  IntBitOr,
  IntBitXor,
  IntEq,
  IntOrd,
  IntDrop,
  IntClone,
  IntMethods
;