{ Drop } :: import "../../modules/common.yo";


implicit(DropArray) :: ((
  forall(compt(T): Type, compt(S): usize), 
  implicit(compt(DropT): Drop(T)))
  -> compt(Drop(Array(T, S))))
  Drop(Array(T, S))
    drop : 
      fn(self)-> 
        for self, elem => {
          DropT.drop(elem);
        };
  ;

export DropArray;