# 可流动性（Flowability）—— 默认安全的引用

Yo 在编译期保证：安全代码无法构造出悬垂引用。这一设计是**构造即正确**
（sound by construction）：安全语言中根本没有任何方式能形成指向可重分配
存储的指针，因此经典的失效陷阱（增长列表导致借用悬垂）不是被某种聪明的
分析"拒绝"的 —— 它们*无法表达*。

## `ref` 能出现在哪里

`ref` 是 Yo 的二等引用，它只存在于**一个**位置：**参数位置**。
`ref(name) : T` 接收调用者的左值（可写回，大结构体免拷贝）。接收 ref
的回调参数（`body : Impl(Fn(ref(v) : T) -> R)`，如 `Mutex.with_lock`）
是同一机制向下传递一层。

**函数不能返回 `ref`**，也**没有局部 ref 绑定**（`ref(r) := …` 会被
拒绝并附带迁移指引 —— 字段本来就能就地读写：`h.s = v`；绑定句柄
`b := a.b` 即可保持对象存活）；ref 也不能存入字段、被闭包捕获、或出现
在泛型类型内。ref 在调用边界出生、在调用返回时消亡 —— 它永远不可能比
它指向的存储活得更久。

传给 `ref` 参数的实参是一个简单的左值**位置（place）**：

- 完整变量（任何作用域 —— 变量槽位是稳定存储）；
- 以**局部变量或参数**为根的 `var.field`（或 struct 字段路径）——
  该位置位于根对象的分配内部，调用者的句柄会让它在整个调用期间存活；
- 经过中间 **object**（如 `a.b.s` 中 `b` 是 object 字段）的链，以及
  以**模块级**变量为根的字段链都会被拒绝 —— 被调用者可能替换该句柄的
  槽位并释放被借用的存储。修复只需一行：先把对象绑定到局部
  （`b := a.b`）—— 局部句柄会自然地钉住它；
- **索引元素**（`xs(i)`）或经过**中间 object** 的链（包括 `Box` 解引用
  `b.*` —— `Box` 是普通 object，`*` 只是字段名）是指向堆对象存储内部的
  指针；只有当被调用者无法接触到该对象时才能作为 ref 实参 —— 同一次
  调用中传入该容器/box（或别名）、索引模块级容器，或传入**任何其他
  object/闭包实参**（可能持有它的句柄），都会被拒绝（增长或重新赋值会
  在引用之下释放存储）。仅元素的用法（`to_string(xs(i))`、`${xs(i)}`、
  `bump(xs(i))`、`owner_box.*.id.clone()`）安全且合法；要在一次调用中
  同时使用元素和容器，先用 `.get(i)` 把元素拷出来。

> **一处已记录的残留。** 这个基于实参可达性的检查看不到一个先逃逸进
> **全局**堆结构的容器（`g.push(xs); bump(xs(i))`，其中 `bump` 通过全局
> 增长 `xs`）—— 完全封闭它需要全程序逃逸分析。该形态相当刻意，普通代码
> 永远不会触发；见 `issues/ref-arg-heap-escape-to-global-residual.md`。

## 元素访问：句柄与拷贝，而非内部指针

容器只交出**值**，从不交出指向其缓冲区内部的指针：

```rust
e := xs.get(i);          // object 元素：指向元素对象的句柄
e.push_str("!");         //   就地变异元素；句柄在 xs.push / realloc 之后
                         //   依然有效 —— 它指向 String 对象本身，
                         //   而不是 xs 的缓冲区内部
xs(i) = v;               // 索引写：就地替换元素
t := xs.get(i).unwrap(); // struct 元素：拷出 ……
xs(i) = t2;              //   …… 再写回
for(xs, (x) => { ... }); // 迭代是值形式（into_iter）
```

`project`、`Indexable`、以及 `for` 的借用形式都已不存在 ——
`for(coll, ref(x) => …)` 会产生带迁移指引的编译错误。`str` 仍然是永生的
静态字节视图（自由复制、无约束），区间索引**拷贝**（`arr(a..b)` 返回新
`ArrayList`），所以修改源永远不会影响结果。

## 一条调用点规则

**同一次调用不得把同一个对象同时作为 ref 扎根的实参和 `own` 实参传入**
（`use_and_sink(h.s, h)` 配 `own(victim)` 会被拒绝）—— `own` 把调用者
的引用计数移交给被调用者，而后者可能在借用仍被使用时释放它。不同对象
没有问题；按值重叠也没有问题：借用的句柄永远无法释放调用者的计数
（把它转交给 `own` 位置会先 dup）。

没有了局部绑定，就没有任何东西可"失效"：旧的借用失效门已随绑定形式
一并删除。`get` 返回的元素句柄在任何容器操作之后都有效 —— 即使容器被
重新赋值 —— 因为句柄对元素对象持有 +1。

## 逃逸通道

`pragma(Pragma.AllowUnsafe);` 豁免整个文件（标准库经过审计的内部实现使
用它）；`unsafe(expr)` 标记单个表达式。裸指针 `Index` 实现
（`index : … -> *(T)`）是热循环确有测量需求时受支持的零拷贝逃逸通道。
参见 [MEMORY_SAFETY.md](./MEMORY_SAFETY.md)。

## 测试

`tests/ref_binding.test.yo`、`tests/ref_local_binding.test.yo`、
`tests/ref_params.test.yo`、`tests/ref_field_borrow.test.yo`（含所有者
pin 与 ref/own 调用门）、`tests/ref_borrow_invalidation.test.yo`（门矩
阵 + get 句柄模型）、`tests/ref_return_ban.test.yo`、以及
`tests/flowability_comprehensive.test.yo`。
