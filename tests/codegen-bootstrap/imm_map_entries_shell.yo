{ assert } :: import("std/assert");
{ Map, Pair } :: import("std/imm/map");
{ List } :: import("std/imm/list");
main :: (fn() -> unit)({
  m := Map(i32, i32).new();
  es := m.entries();
  assert(es.len() == usize(0), "x");
});
export(main);
