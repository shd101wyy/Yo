pragma(Pragma.AllowUnsafe);
open(import("std/libc/stdio"));
main :: (fn() -> unit)({
  (i : i32) = i32(1);
  (sum : i32) = i32(0);
  while(i <= i32(5), {
    sum = (sum + i);
    i = (i + i32(1));
  });
  unsafe(_a := putchar(int(sum + i32(50))));
});
export(main);
