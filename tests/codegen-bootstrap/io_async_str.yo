{ println } :: import("std/fmt");
run :: (fn(io : Io) -> str)({
  task := io.async((io : Io) => "hello async");
  io.await(task, io)
});
main :: (fn(io : Io) -> unit)({
  println(run(io));
});
export(main);
