open(import("std/string"));
open(import("std/fmt"));

Raise :: struct(
  raise : (ctl(msg : String) -> i32)
);

main :: (fn() -> unit)({
  outer_val := i32(42);
  comptime_expect_error(
    (r : Raise) = Raise(
      raise : ((msg) -> outer_val)
    )
  );
  println(`ok`);
});
export(main);
