{ String } :: import("std/string");
{ println } :: import("std/fmt");
main :: (fn() -> unit)({
  s := String.from("abc");
  t := s.clone();
  s.push_str("d");
  println(s.len());
  println(t.len());
});
export(main);
