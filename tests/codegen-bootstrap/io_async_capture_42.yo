{ println } :: import("std/fmt");
run :: (fn(io : Io) -> i32)({
  base := i32(42);
  task := io.async((io : Io) => base);
  io.await(task, io)
});
main :: (fn(io : Io) -> unit)({
  println(run(io));
});
export(main);
