// V3 negative fixture (plans/backlog/FORMAL_VERIFICATION.md task 8).
//
// UNANNOTATED code — no contracts anywhere. The AoRTE automatic
// obligation (divisor non-zero) is the only thing that fires, and it
// REFUTES: y = 0 divides by zero. This is the plan's flagship claim —
// bounds/div-by-zero bugs become compile errors on code nobody
// annotated. The correct twin is `safe_div` in
// tests/spec/verify_straight_line.test.yo (requires(y != 0), proves).
pragma(Pragma.Verify);

divide_bugged :: (fn(x : i32, y : i32) -> (r : i32))(x / y);
