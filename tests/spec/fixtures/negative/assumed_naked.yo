// V6 task 5 negative fixture (SIGNATURE error — no solver needed):
// `assumed()` with no contract clause anywhere — there is nothing to
// assume. Must FAIL to load with the 'assumed()' without any contract
// clause diagnostic.
pragma(Pragma.Verify);

f :: (fn(x : i32, assumed()) -> (r : i32))(x);
