// B1: a law must CLAIM something. Without an `ensures(...)` there is no
// obligation, and the task would report a vacuous pass — exactly the
// "green because nothing was checked" failure laws exist to prevent.
pragma(Pragma.Verify);

bad_law :: law(fn(x : i64, requires(x >= i64(0))) -> unit);
