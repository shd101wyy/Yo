// B1: `assumed()` on a law would make it report the PASSING outcome `assumed`
// with zero obligations — a claim that proves itself by declaring it need not
// be proved. Rejected at the signature.
pragma(Pragma.Verify);

bad_law :: law(fn(x : i64, ensures(x >= i64(0)), assumed()) -> unit);
