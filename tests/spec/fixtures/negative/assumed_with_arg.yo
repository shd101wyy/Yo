// V6 task 5 negative fixture (SIGNATURE error — no solver needed):
// `assumed()` with an argument. The marker takes no arguments; the
// contracts come from requires/ensures/decreases. Must FAIL to load
// with the 'assumed()' takes no arguments diagnostic.
pragma(Pragma.Verify);

f :: (fn(x : i32, requires(x > i32(0)), assumed(x)) -> (r : i32))(x);
