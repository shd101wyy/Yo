// B1: a law has no VALUE — its claim lives in `ensures(...)`, so the signature
// must return unit. A non-unit return would give `ensures` a return label to
// bind that no caller could ever supply.
pragma(Pragma.Verify);

bad_law :: law(fn(x : i64, ensures(x >= i64(0))) -> i64);
