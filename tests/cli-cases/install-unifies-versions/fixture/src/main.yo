{ println } :: import("std/fmt");
app_lib :: import("app_lib");
util :: import("util");
main :: (fn() -> unit)({
  println(app_lib.describe());
  println(`root sees util ${util.version()}`);
});
export(main);
