# The mirror (git/), the tag index (index/) and the projects registry are keyed
# by the sandbox path of the remote / project, so their names and contents
# differ per run; the store trees (store/) are content-addressed and compared.
./.cache/yo/git/*
./.cache/yo/index/*
./.cache/yo/projects
