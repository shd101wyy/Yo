# plans/BUILD_AND_DEPENDENCY_SYSTEM_REDESIGN.md §4.7 (B11): build names share
# ONE namespace, because `resolve_dependency` resolves a bare name by trying
# each registry in turn and taking the first hit. Only artifacts were checked
# for duplicates, and only against other artifacts — so a step, test, run or
# doc step could take a name already in use and silently become unreachable.
timeout=300
