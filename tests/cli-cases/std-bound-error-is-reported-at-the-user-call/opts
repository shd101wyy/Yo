# A where-clause failure inside std is reported at the user's call, with the std
# location as a note; it used to point only at std/fmt's where clause (Phase 4.4).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+|note: .*
timeout=120
