// Sorts AFTER prelude.yo, so the walker reaches it after the prelude was
// already checked as a target — the order that used to corrupt the
// generic-impl registry (this exact specialization then failed with
// "No matching call found", the GATE 3 signature).
buf :: Array(u8, usize(4)).fill_pin(u8(7));
