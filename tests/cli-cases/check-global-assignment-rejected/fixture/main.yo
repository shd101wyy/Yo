{ println } :: import("std/fmt");
(counter : i32) = i32(0);
bump :: (fn() -> unit)({
  counter = (counter + i32(1));
});
main :: (fn() -> unit)({
  bump();
  println(`counter=${counter}`);
});
export(main);
