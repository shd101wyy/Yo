# Parallelism soundness Phase 3 (rule D1): a Send value-typed global is fine to
# READ from any thread, but assigning it anywhere makes it a mutable static
# (`static mut` in Rust): rejected in a safe non-test file. thread_local(counter)
# is the per-thread spelling; an AtomicI32 the shared one.
stdout_keep_match=Cannot assign to module-level global 'counter'
timeout=120
