# Two closures are two types (plans/reference/TYPE_IDENTITY.md, "closure
# identity"). Every closure's `Impl(Fn(...))` wrapper carries the name "Impl",
# so identity's name + level rule made `ArrayList(typeof(k1))` accept `k2`: check
# passed, and only clang caught the mismatch
# (issues/fixed/a-container-of-a-closure-type-does-not-compile.md). A
# check-level gate: a comptime_expect_error block observes a swallowed trial,
# so it cannot pin "check passes wrongly".
stdout_keep_match=Cannot unify incompatible struct types
timeout=120
