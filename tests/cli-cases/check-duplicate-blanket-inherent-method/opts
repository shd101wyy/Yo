# The duplicate-inherent rule for a GENERIC receiver: two blanket impls over the same
# bound defining one name used to register side by side, the first winning dispatch
# (issues/fixed/wrapping-methods-shadowed-by-a-loop-based-duplicate.md shipped a 4x slowdown
# that way; issues/fixed/yo-self-missing-duplicate-impl-checks.md).
stdout_keep_match=already defined for type
timeout=120
