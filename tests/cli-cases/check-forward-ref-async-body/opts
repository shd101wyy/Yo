# P0 of plans/LAZY_TOPLEVEL_BINDINGS.md pinned this shape — a forward-referenced
# call inside an io.async closure body — as a check-time ERROR, because the
# def-time trial used to swallow it into a hollow stub (the class that shipped
# silently-broken `yo version` code up to v0.2.20). Since P1 landed, `later` is
# a deferred `::` definition: the reference FORCES it and check passes. The
# case now pins that outcome (rc 0, no diagnostic) — a regression to either
# the silent hollow or the old error shows up as a golden diff.
stdout=strict
timeout=120
