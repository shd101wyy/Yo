{ RawMutex } :: import("std/sync/mutex");
{ println } :: import("std/fmt");
main :: (fn() -> unit)({
  r := RawMutex.new();
  println(`held=${r.held_by_current_thread()}`);
  r.unlock();
  println(`never`);
});
export(main);
