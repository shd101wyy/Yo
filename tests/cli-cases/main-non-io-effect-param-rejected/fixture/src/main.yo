{ Exception } :: import("std/error");
{ println } :: import("std/fmt");

main :: (fn(io : Io, exn : Exception) -> unit)({
  println("never reached: the signature alone is the error");
});
export(main);
