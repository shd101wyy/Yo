{ println } :: import("std/fmt");

make :: (fn(flag : bool) -> Option(i32))({
  return(
    cond(
      flag => Option(i32).Some(i32(7)),
      true => Option(i32).None
    )
  );
});

main :: (fn() -> unit)({
  r := make(true).unwrap();
  println(`r=${r}`);
});
export(main);
