# `yo effects` (audit §3.6): the effect capabilities each EXPORTED fn requires,
# read off the function types — `ctl` for a handler record (Exception), `io`
# for the prelude Io, both for the IoExn bundle, and the effects of a returned
# Impl(Future(T, E)) under `future`. The private helper is not exported and
# does not appear. Both renders, human then --json; rc=0 for both.
timeout=180
