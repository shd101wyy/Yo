# `--error-format json` carries the mechanical repair as a STRUCTURED field,
# so an agent applies the fix without parsing prose
# (plans/LLM_FRIENDLY_TOOLCHAIN_AND_SYNTAX.md §2). The human renderer's
# "did you mean" has always been there; this pins that the machine-readable
# channel now says WHERE to edit and WITH WHAT.
stdout_keep_match=replacement
timeout=180
