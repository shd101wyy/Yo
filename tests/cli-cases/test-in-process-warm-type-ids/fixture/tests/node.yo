/// A recursive generic struct: inside its own definition `Self` is a
/// zero-field shell carrying the SAME struct id as the final form.
Node :: (fn(comptime(T) : Type) -> comptime(Type))(ref(struct(value : T, next : Option(Self))));
export(Node);
