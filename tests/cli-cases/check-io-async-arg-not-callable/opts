# The io.async half of the same defect, and a DIFFERENT call path: this one
# reaches check_if_function_parameter_matches_argument (calls/helper.yo), where
# Step-6 synthesis bound the block's type into the `Impl(Fn(e : E) -> T)` type
# variable and Step 8 then compared i32 against i32. The keep-match pins the
# parameter name so the case asserts THE failure, not just A failure.
stdout_keep_match=Argument for parameter "action" is not callable
timeout=120
