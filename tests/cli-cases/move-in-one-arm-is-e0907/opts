# A value moved in one arm and read in its sibling is moved in some arms only (E0907),
# not a use-after-move in the sibling (Phase 5.1).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
