# Raw pointer type in a safe-code signature (port of unsafe-gate.test.ts):
# `fn(p : *(i32))` without pragma(Pragma.AllowUnsafe) must FAIL under BOTH
# compilers. This is the SIGNATURE-level gate — it fires eagerly, outside the
# def-eval swallow that still masks the body-level &(x)/unsafe() gates
# self-hosted (see issues/self-hosted-compile-swallows-undefined-call.md;
# those two negatives are deliberately NOT cases yet). stdout=ignore: the
# binaries identify fatal errors differently; rc parity is the assertion.
stdout=ignore
timeout=120
