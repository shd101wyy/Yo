# plans/TYPE_SYSTEM_SOUNDNESS.md Phase 4.5: ranges and constants that cover a
# fixed-width integer type are exhaustive without `_` (they used to need one,
# E0607). A trailing `_` after them is still accepted.
stdout_keep_match=(error\[E0[0-9]+\]|warning: [a-z ]+)
