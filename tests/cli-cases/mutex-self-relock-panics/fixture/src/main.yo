{ Mutex } :: import("std/sync/mutex");
{ println } :: import("std/fmt");
main :: (fn() -> unit)({
  m := Mutex(i32).new(i32(1));
  outer := m.with_lock(v => {
    println(`outer holds v=${v}`);
    m.with_lock(w => w)
  });
  println(`never ${outer}`);
});
export(main);
