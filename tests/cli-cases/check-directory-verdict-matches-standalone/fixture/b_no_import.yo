// Never imports std/fmt. Alone, `yo check` rejects `x.to_string()` (E0610);
// beside a.yo in one directory check it must be rejected the same way.
{ String } :: import("std/string");
h :: (fn(x : i64) -> String)(x.to_string());
export(h);
