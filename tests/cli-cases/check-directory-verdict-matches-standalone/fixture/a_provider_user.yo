// Imports the provider of integer `to_string` (std/fmt's ToString impls) and
// forces it, so the impl is registered in this process before b.yo is checked.
{ String } :: import("std/string");
{ ToString } :: import("std/fmt");
g :: (fn() -> String)(i64(7).to_string());
export(g);
