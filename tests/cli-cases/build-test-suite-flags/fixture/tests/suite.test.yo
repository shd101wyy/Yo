{ assert } :: import("std/assert");
test("first passes", {
  assert(true, "first");
});
test("second fails on purpose", {
  assert(false, "deliberate failure — the suite bails here");
});
test("third must never run under bail", {
  assert(true, "third");
});
