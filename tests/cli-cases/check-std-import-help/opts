# The help channel of the E0401 std-import repair: the human rendering names
# the module and the import line; `--error-format json` carries the same
# repair structurally (span at row 1 — below the `//!` line — length 0, the
# import line as the replacement) for the LSP / an agent to apply.
