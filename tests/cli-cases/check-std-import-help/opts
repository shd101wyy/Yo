# The help channel of the E0401 std-import repair: the human rendering names
# the module and the import line. The structured form is
# check-std-import-repair-json (one command per case: the harness stops at
# the first non-zero rc, and both fail by design).
