# A value moved inside a loop body and read by the next iteration is E0901 at the
# move; it compiled and read freed memory (Phase 5.1).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
