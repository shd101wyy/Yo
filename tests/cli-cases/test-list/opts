# `yo test --list` enumerates the collected + filtered tests WITHOUT compiling
# anything: one `<file>\t<name>` line each, or a `test` event per line under
# --json. The name filter applies, so an agent can preview what a pattern
# selects before paying for the batch compile.
