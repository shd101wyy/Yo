# The borrowed `for(xs, inout(x) => ...)` in an awaiting `io.async` body is E0904 at
# `check` time: the evaluator's walk follows the `for` macro's expansion (Phase 4.4).
stdout_keep_match=error\[E0904\]|--> main\.yo:[0-9]+:[0-9]+
timeout=120
