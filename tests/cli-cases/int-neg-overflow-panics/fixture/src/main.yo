{ println } :: import("std/fmt");

negate :: (fn(a : i64) -> i64)({
  return(-a);
});

main :: (fn() -> unit)({
  bottom := (i64(-9223372036854775807) - i64(1));
  println(`in-range=${negate(i64(5))}`);
  r := negate(bottom);
  println(`never=${r}`);
});
export(main);
