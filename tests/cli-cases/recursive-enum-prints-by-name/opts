# A recursive enum's self-shell prints as the enum's name, not an internal
# `__self_shell` id (Phase 4.4).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+|help: did you mean.*
timeout=120
