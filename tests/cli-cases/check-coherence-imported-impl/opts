# plans/reference/TRAIT_COHERENCE.md rule 2: an impl an imported module (here std/fmt)
# already registered makes a local impl of the same (type, trait) a duplicate.
stdout_keep_match=explain E0612|--> main\.yo
timeout=120
