# A variable initialized in only some arms of a `cond` is E0903 at the join; the check
# never fired because every arm saw the first arm's assignment (Phase 5.6).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+|(Might|Not) .*
timeout=120
