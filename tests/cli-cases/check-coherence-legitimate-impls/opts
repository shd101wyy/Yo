# plans/reference/TRAIT_COHERENCE.md canary: nothing here is a second impl. Two structs
# at the SAME row and column of different modules each implement `Show` (their type ids
# used to collide — issues/fixed/trait-ids-omit-the-module-so-two-traits-can-share-one-id.md),
# a blanket impl whose where-clause excludes a concretely-implemented type coexists with that
# impl, one generic impl covers Wrap(i32) and Wrap(bool), a user type opts into `Format`, and
# two blanket inherent impls over DIFFERENT bounds define the same method name.
timeout=120
