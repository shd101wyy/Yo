$ yo context --path src lib add
Building the context index for <PROJ>/src (key <CONTEXT_KEY>)...
  [1/3] indexed
  [2/3] indexed
  [3/3] indexed
src/lib — add
  (re-exported from src/alpha)

fn  fn(a : i32, b : i32) -> i32

### `add`

```rust
fn(a : i32, b : i32) -> i32
```

Adds two numbers.

**Parameters:**

- `a` : `i32`
- `b` : `i32`

**Returns:** `i32`
rc=0
$ yo context --path src add
src/alpha — add

fn  fn(a : i32, b : i32) -> i32

### `add`

```rust
fn(a : i32, b : i32) -> i32
```

Adds two numbers.

**Parameters:**

- `a` : `i32`
- `b` : `i32`

**Returns:** `i32`
rc=0
$ yo context --path src lib add --format json
{"module":"src/lib","origin":"src/alpha","name":"add","kind":"fn","signature":"fn(a : i32, b : i32) -> i32","doc":"Adds two numbers.","text":"### `add`\n\n```rust\nfn(a : i32, b : i32) -> i32\n```\n\nAdds two numbers.\n\n**Parameters:**\n\n- `a` : `i32`\n- `b` : `i32`\n\n**Returns:** `i32`"}
rc=0
