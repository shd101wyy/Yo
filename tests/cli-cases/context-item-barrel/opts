# A barrel re-export (`lib/index.yo` spreads `alpha`) is described from the
# module that DEFINES it — its doc and page — with a re-exported note; the
# bare name counts the two exposures once (plans/reference/YO_CONTEXT.md C7).
env=YO_CONTEXT_CACHE=<PROJ>/cache
