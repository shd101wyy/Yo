# plans/backlog/LLM_AUTHORING_AUDIT_2026-09-19.md §2.2: a bare-variant local
# with NO later return/tail use still cannot be inferred; the diagnostic now
# carries the repair options as help.
stdout_keep_match=(Failed to infer enum variant type|help: .*)
