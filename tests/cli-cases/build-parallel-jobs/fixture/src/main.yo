pragma(Pragma.AllowUnsafe);
{ println } :: import("std/fmt");
extern("Yo", alpha : (fn(n : i32) -> i32));
extern("Yo", beta : (fn(n : i32) -> i32));

main :: (fn() -> unit)({
  println((alpha(i32(10)) + beta(i32(10))).to_string());
});

export(main);
