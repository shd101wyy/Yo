# plans/INOUT_LOCAL_BINDINGS_AUDIT.md §9: the alias the compiler cannot see.
# The collection being iterated by a borrowed `for` is grown through a SECOND
# handle stored in a module-level structure and reached by a callee; the
# runtime borrow flag on the shared object panics at `push` instead of
# reallocating the buffer under the live element reference.
stdout_keep_match=panic: container operation while an interior reference.*
timeout=300
