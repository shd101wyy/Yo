{ println } :: import("std/fmt");
{ args } :: import("std/env");

main :: (fn() -> unit)({
  argv := args();
  (i : usize) = usize(1);
  while(i < argv.len(), {
    println(argv(i));
    i = (i + usize(1));
  });
});

export(main);
