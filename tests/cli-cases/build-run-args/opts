# plans/BUILD_AND_DEPENDENCY_SYSTEM_REDESIGN.md §4.7 (B9): a `run` step could
# not be given arguments — `BuildRunStep.args` was always empty and the CLI had
# no `--`. Everything after `--` now reaches the program, in order, after any
# arguments the build file itself declared. The program echoes its argv.
timeout=600
stdout_keep=^(alpha|beta|Building |Error|error:)
