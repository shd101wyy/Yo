# `build run`: the emitted C differs between the two compilers, so keep only
# the program's own output and the error lines. The case asserts that a
# module-level initializer calling a method on a user function's result is
# collected, emitted and evaluated at startup (g_on=true g_off=false).
timeout=900
stdout_keep=^(g_on=|Error|error:)
