# plans/TYPE_SYSTEM_SOUNDNESS.md Phase 4.5: over `Value(i32)`, a `.BoolVal` arm
# (a variant that constructs `Value(bool)`) can never run (E0608). The same arm
# in a generic `Value(T)` function is fine; tests/gadts.test.yo covers that.
stdout_keep_match=(error\[E0[0-9]+\]|Unreachable match arm: [^ ]+)
