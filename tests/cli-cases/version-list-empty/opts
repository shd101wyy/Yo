# `yo version list` with an EMPTY cache (the sandbox has its own HOME, so
# ~/.cache/yo/versions does not exist). Deterministic, no network: the P3
# item-2 redesign (GitHub-Releases bundles) must agree between compilers on
# the cache-miss message and rc. Companion network case:
# version-install-pinned.
timeout=120
