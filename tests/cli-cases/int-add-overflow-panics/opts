# Safe mode Phase 3a (plans/SAFE_MODE.md §6, D1): i32 MAX + 1 at runtime aborts
# with a located diagnostic instead of wrapping (-fwrapv made it defined but
# silently wrong). Recorded by hand: the recording binary must carry the trap.
stdout_keep_match=integer addition overflow
timeout=300
