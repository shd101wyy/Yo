{ println } :: import("std/fmt");

add :: (fn(a : i32, b : i32) -> i32)({
  return(a + b);
});

main :: (fn() -> unit)({
  top := i32(2147483647);
  println(`in-range=${add(i32(1), i32(2))}`);
  r := add(top, i32(1));
  println(`never=${r}`);
});
export(main);
