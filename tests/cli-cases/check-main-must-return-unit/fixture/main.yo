{ println } :: import("std/fmt");
// A `main` that reports failure through its result: accepted and silently
// discarded before 2026-09-16, now a compile error.
main :: (fn() -> i32)({
  println("work");
  i32(3)
});
export(main);
