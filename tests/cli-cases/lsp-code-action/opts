# `textDocument/codeAction` serves the compiler's own `Repair` as a quickfix
# (audit §3.2): the E0401 rename `countr` → `counter` arrives as a WorkspaceEdit
# whose text edit is byte-for-byte what `yo fix` would apply. A range on a line
# without a repair-carrying diagnostic (request 3) answers an empty list, not
# an error. Also pins `codeActionProvider` in the initialize result.
timeout=240
