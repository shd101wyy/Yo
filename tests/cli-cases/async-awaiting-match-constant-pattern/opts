# A constant pattern in a match whose arms await is a coded user error
# (Phase 4.1), not an internal compiler error.
stdout_keep_match=error\[E0[0-9]+\]|internal compiler error|whose arms await
timeout=120
