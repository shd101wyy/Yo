# `::` over an `if` whose condition is a runtime parameter is not a compile-time
# value: it passed check and emitted C that named an undeclared `N` (Phase 4.4).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
