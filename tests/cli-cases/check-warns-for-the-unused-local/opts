# The warnings channel (P4/D15): `check` surfaces an `unused variable`
# warning for the initialized local nothing ever reads, and STAYS SILENT
# about `y`, which is used — one keep-match, so the golden also fails if
# the used local starts warning. Warnings never change the exit code.
stdout_keep_match=warning: unused variable
timeout=180
