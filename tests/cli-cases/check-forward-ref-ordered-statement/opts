# plans/LAZY_TOPLEVEL_BINDINGS.md rules 6/7: only `::` definitions and `impl`
# registrations are order-independent. A module-level RUNTIME global stays an
# ordered statement, so a body that reads one bound LATER is still a forward
# reference — and the P0 diagnostic must say which kind of statement it is.
stdout_keep_match=forward reference to "counter" \(bound at line 3\)
timeout=120
