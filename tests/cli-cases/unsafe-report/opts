# The unsafe-surface audit (port of src/unsafe-report.ts): both the human
# format and --json must be BYTE-IDENTICAL between compilers (verified on the
# whole std/ tree during the port; this case pins it in CI on a fixture with
# a SAFETY comment, deref/addr-of subkinds, and a privileged + a clean file).
timeout=120
