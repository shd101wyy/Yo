# A `cond` that picks a Type on a runtime condition has no meaning: it passed check
# and hollowed the enclosing function into an abort stub (Phase 4.4).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
