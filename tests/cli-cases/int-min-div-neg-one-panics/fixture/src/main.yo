{ println } :: import("std/fmt");

div :: (fn(a : i32, b : i32) -> i32)({
  return(a / b);
});

main :: (fn() -> unit)({
  bottom := (i32(-2147483647) - i32(1));
  println(`in-range=${div(i32(-7), i32(2))}`);
  r := div(bottom, i32(-1));
  println(`never=${r}`);
});
export(main);
