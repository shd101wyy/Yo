# Safe mode Phase 2 (H5): i32 MIN / -1 is UB in C (SIGFPE on x86); the
# signed division guard names the MIN / -1 case. Recorded by hand.
stdout_keep_match=integer division or remainder by zero \(MIN / -1\)
timeout=300
