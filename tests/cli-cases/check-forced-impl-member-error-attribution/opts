# plans/reference/LAZY_TOPLEVEL_BINDINGS.md §4.5 / §8: a sibling method forced out of
# order from inside its own impl block (the `self.helper()` miss in `double`)
# that has a GENUINE error must report that error at its own position (not a
# misleading "no method" at the use site) and say why it ran early.
stdout_keep_match=note: `helper` was evaluated here because a sibling method of this impl references it before its definition
timeout=120
