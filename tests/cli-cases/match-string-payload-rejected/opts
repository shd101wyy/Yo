# plans/MATCH_PATTERN_MATCHING.md P3 (2026-09-19): a string literal in a payload
# position COMPARES (it was rejected with "not supported in a payload position"
# before the Pattern IR). This check-level case pins that the fixture is accepted;
# tests/match_strings.test.yo pins the runtime behaviour.
stdout=ignore
