# issues/fixed/async-inout-check-skips-cond-and-match-arms.md: an `inout(name) :=`
# in a `cond` arm of an awaiting `io.async` body is E0904 at `check` time; the
# evaluator's walk used to skip every `=>`, arms included.
stdout_keep_match=error\[E0904\]|--> main\.yo:[0-9]+:[0-9]+
timeout=120
