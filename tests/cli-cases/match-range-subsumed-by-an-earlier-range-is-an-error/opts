# plans/TYPE_SYSTEM_SOUNDNESS.md Phase 4.5: range patterns are intervals, so
# `(5..=7)` after `(0..10)` is subsumed by that one arm (E0608).
stdout_keep_match=(error\[E0[0-9]+\]|Unreachable match arm: [^ ]+)
