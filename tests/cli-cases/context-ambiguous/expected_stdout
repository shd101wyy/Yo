$ yo context --path src add
Building the context index for <PROJ>/src (key <CONTEXT_KEY>)...
  [1/3] indexed
  [2/3] indexed
  [3/3] indexed
'add' names 2 items — yo context <module> add for one:
  src/alpha  add  fn  fn(a : i32, b : i32) -> i32
  src/gamma  add  fn  fn(x : i32) -> i32
rc=0
$ yo context --path src add --verbose
'add' names 2 items — yo context <module> add for one:
  src/alpha  add  fn  fn(a : i32, b : i32) -> i32
  src/gamma  add  fn  fn(x : i32) -> i32

src/alpha — add

fn  fn(a : i32, b : i32) -> i32

### `add`

```rust
fn(a : i32, b : i32) -> i32
```

Adds two numbers.

**Parameters:**

- `a` : `i32`
- `b` : `i32`

**Returns:** `i32`

src/gamma — add

fn  fn(x : i32) -> i32

### `add`

```rust
fn(x : i32) -> i32
```

Adds one.

**Parameters:**

- `x` : `i32`

**Returns:** `i32`
rc=0
