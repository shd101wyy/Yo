# Safe mode Phase 3b (§6): a shift count >= the operand's width was C UB.
# The count is guarded per the type's width; the message names the range.
# Recorded by hand: the recording binary must carry the guard.
stdout_keep_match=shift count out of range: 64 not in \[0, 64\)
timeout=300
