# An unknown subcommand (WITHOUT --help — `help-unknown-subcommand` covers the
# fallback to the usage text) prints the dispatch list. The list is
# SUBCOMMAND_NAMES in src/main.yo and must match what `main` answers: it
# carried a phantom `fetch` and omitted explain/add/remove/update.
