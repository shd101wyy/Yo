# plans/BUILD_AND_DEPENDENCY_SYSTEM_REDESIGN.md B13: `build.documentation`
# carried `logo` and `favicon` from the day it was written and the renderer
# read neither — the fields travelled to `BuildDocConfig` and stopped there.
# They now reach `DocModel`, so the sidebar gets an `<img class="logo">` and
# the page head a `<link rel="icon">`; `yo doc` grew the matching `--logo` /
# `--favicon` flags, which is what this case drives.
timeout=300
