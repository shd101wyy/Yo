# plans/LAZY_TOPLEVEL_BINDINGS.md rule 4: two constants whose values depend on
# each other are a cycle — forcing B from A's RHS re-enters A, which has no
# phase-A value (only fn definitions do). The keep-match pins the chain text.
stdout_keep_match=cyclic definition: A \(line 2\) → B \(line 3\) → A
timeout=120
