pragma(Pragma.AllowUnsafe);
{ assert } :: import("std/assert");
open(import("std/fmt"));
main :: (fn() -> unit)({
  x := i32(42);
  p := &x;
  v := unsafe(p.*);
  assert(v == i32(42), "deref via unsafe");
  println(`v = ${v}`);
});
export(main);
