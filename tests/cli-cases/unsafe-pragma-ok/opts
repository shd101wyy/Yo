# pragma(Pragma.AllowUnsafe) + unsafe(...) wrap compiles AND RUNS (the positive
# arm of unsafe-gate.test.ts): address-of + unsafe deref work end-to-end under
# BOTH compilers. The kept "v = 42" line is program output — a binary that
# never executed cannot pass. Build chatter differs between compilers, hence
# the keep filter.
stdout_keep=^v = 
timeout=300
