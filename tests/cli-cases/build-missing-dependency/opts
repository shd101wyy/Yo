# plans/BUILD_AND_DEPENDENCY_SYSTEM_REDESIGN.md §4.7 (B6): a dependency name
# that resolves to nothing was dropped from the DAG without a word, so
# `install.depend_on(<typo>)` was a no-op and the build exited 0 having built
# nothing. It must name the step and the missing name instead.
timeout=300
