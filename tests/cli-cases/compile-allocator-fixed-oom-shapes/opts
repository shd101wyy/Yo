# plans/SAFE_MODE.md §7 (Phase 4) / §14 R5: the tiny-heap oracles for async
# task spawn, thread spawn and String growth. Each child must die with an
# allocation diagnostic (never a segfault); the parent prints one verdict line
# per shape and exits 0.
stdout_keep=^(task|thread|string): 
timeout=600
