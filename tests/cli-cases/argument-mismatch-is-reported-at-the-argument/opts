# An argument that does not fit its parameter is reported at the argument; the
# definition-time re-raise used to anchor it on the enclosing body's `{` (Phase 4.4).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
