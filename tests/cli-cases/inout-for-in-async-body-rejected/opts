# plans/INOUT_LOCAL_BINDINGS_AUDIT.md §9 H5 (v1 limitation): the borrowed `for`
# binds each element as an `inout` local, and `inout` local bindings are not
# supported inside an `io.async` body that is a real state machine (a body
# with a suspension point). The rejection is the same codegen check as
# inout-binding-in-async-body-rejected; a body without an `await` is lowered
# as a plain function and is unaffected. Lifting this needs the element
# pointer spilled into the state struct — a documented follow-up.
stdout_keep_match=is not supported inside an 'io.async' body yet
timeout=120
