{ println } :: import("std/fmt");

div :: (fn(a : i32, b : i32) -> i32)({
  return(a / b);
});

main :: (fn() -> unit)({
  z := i32(0);
  r := div(i32(10), z);
  println(`never=${r}`);
});
export(main);
