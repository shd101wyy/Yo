{ println } :: import("std/fmt");
apply :: (fn(f : Impl(Fn(x : i32) -> i32), v : i32) -> i32)(f(v));
main :: (fn() -> unit)({
  k := i64(10);
  r := apply(x => (x + k), i32(3));
  println(`${r}`);
});
export(main);
