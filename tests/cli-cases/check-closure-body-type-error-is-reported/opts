# A closure whose parameters are all concrete has nothing a call could resolve:
# a type error in its body is reported at `check` (Phase 6 step 2), not swallowed
# into an abort() stub (issues/fixed/closure-body-type-errors-are-swallowed-into-a-runtime-abort.md).
stdout_keep_match=error\[E0601\]|--> main\.yo:[0-9]+:[0-9]+
timeout=120
