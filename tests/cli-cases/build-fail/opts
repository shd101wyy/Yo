# A failing artifact compile must FAIL the build (rc=1). Both compilers print
# their own evaluator diagnostics for the broken source (different shapes —
# TS adds a JS stack trace), so only the lines this case exists to assert are
# compared: the Building line and the Compilation error marker.
timeout=300
stdout_keep=^(Building |Compilation error)
