# plans/reference/C_INCLUDE_EXTERN_MODULE_VALUE.md §3.3: a bare `c_include(...)` is the
# glob destructure of its module value, and destructuring runs the
# no-shadowing rule — so a C symbol may not silently replace a Yo binding of
# the same name. The v0.2.33 seed accepted this file (rc=0) and let
# `<stdlib.h>`'s `abs` win.
stdout_keep_match=Variable "abs" is already defined here \(variable shadowing is not allowed\)
timeout=120
