open(import("std/fmt"));
open(import("std/string"));
first :: (fn() -> i32)(second());
second :: (fn() -> i32)(String.from("not an i32"));
main :: (fn() -> unit)({
  println(first().to_string());
});
export(main);
