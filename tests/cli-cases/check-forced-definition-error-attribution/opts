# plans/reference/LAZY_TOPLEVEL_BINDINGS.md §8 "Error attribution": a definition forced
# out of order that has a GENUINE error must report that error (not a
# misleading "Variable not found" at the use site) and say why it ran early.
stdout_keep_match=note: `second` \(line 4\) was evaluated here because it is referenced before its definition
timeout=120
