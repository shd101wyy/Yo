# Diagnostic goldens for the P0 match rules (plans/MATCH_PATTERN_MATCHING.md
# §6): the message text is pinned because the diagnostics registry keys by
# message text until P1's codes land.
stdout_keep_match=(not supported in a payload position|not exhaustive|Unreachable match arm|Wildcard pattern)
