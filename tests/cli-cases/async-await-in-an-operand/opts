# An io.await nested in an operand is a user error with a code and a caret,
# not an internal compiler error (Phase 4.1).
stdout_keep_match=error\[E0[0-9]+\]|internal compiler error|is not supported in this position
timeout=120
