# A constant pattern with no `==` against the scrutinee is E0609, not the
# operator argument rule it tripped on the way (E0601 after Phase 2.5).
stdout_keep_match=error\[E0[0-9]+\]|cannot match a value of type
timeout=120
