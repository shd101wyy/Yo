# E0401 for a name that exactly ONE std module exports (`println` → std/fmt):
# the unique repair is the import line, inserted above the first non-comment
# line so the `//!` module doc stays first. The expected_tree pins the
# rewritten file; rc=0 because the repaired file then evaluates.
stdout_keep=^fix:
timeout=180
