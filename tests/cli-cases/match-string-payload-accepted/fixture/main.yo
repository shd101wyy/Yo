{ String } :: import("std/string");
E :: enum(Tag(v : String), Empty);
pick :: (fn(e : E) -> i32)(
  match(
    e,
    .Tag("a") => i32(1),
    .Tag(n) => i32(2),
    .Empty => i32(0)
  )
);
export(pick);
