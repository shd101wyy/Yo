# `--error-format json` carries the E0401 std-import repair structurally: span
# at row 1 (below the `//!` line), col 0 == end_col (an insertion), the import
# line plus newline as the replacement — for the LSP quickfix / an agent to
# apply without re-deriving it.
