# plans/BUILD_AND_DEPENDENCY_SYSTEM_REDESIGN.md §4.7 (B7): `--dry-run` used to
# print one line per REQUESTED step without building a DAG, validating the step
# or detecting a cycle — while the help text promised "resolve the build graph
# without running it". It now resolves the graph and prints it, and the absence
# of any `Building …` line is what asserts that nothing was executed.
timeout=300
