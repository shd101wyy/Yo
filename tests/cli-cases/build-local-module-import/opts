# The `yo init` scaffold's own shape: `build.module` + `exe.add_import` on the
# SAME project, so `import("app")` resolves to ./src/lib.yo by name. Registered
# but never consumed before the `--imports` plumbing
# (plans/archive/P2_5_RETIRE_EXECUTION.md:420: "a yo init project's own
# build.module imports fail").
timeout=600
stdout_keep=^(Building|42$)
