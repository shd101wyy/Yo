// Deliberately minimal: the point of this case is WHICH std the child compile
// resolves, not what the test asserts.
{ assert } :: import("std/assert");
test("std-path reaches the batch compile", {
  assert((i32(1) + i32(1)) == i32(2));
});
