# Member and label definition targets + display names for instantiated
# generics (issues/fixed/lsp-member-definition-targets.md,
# issues/fixed/lsp-shows-internal-type-placeholders-to-users.md): hover on
# `list` / `opt` reads `ArrayList(i32)` / `Option(i32)`; go-to-definition on a
# field access (`p.x`), an inherent method (`p.sum`), an enum variant (`Color.Red`
# and the `.Red` match pattern), a struct-literal label (`Point(x : …)`), a
# generic-impl method in std (`list.push`, `list.len`, `opt.unwrap_or` — the
# std URI is normalized to <REPO>), and a module member (`al.ArrayList`).
# Dot completion on `list.` shows `ArrayList(T)`-shaped details, not
# `<struct:struct_yo_id_N>`.
timeout=240
