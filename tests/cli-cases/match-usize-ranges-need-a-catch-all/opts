# plans/TYPE_SYSTEM_SOUNDNESS.md Phase 4.5: `usize`/`isize` have no portable
# maximum, so no set of ranges covers them (as in Rust): a catch-all is
# required (E0607).
stdout_keep_match=(error\[E0[0-9]+\]|requires a wildcard)
