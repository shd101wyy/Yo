# An inout binding in an io.async body that awaits is rejected by `check`
# (Phase 4.1: the evaluator decides it from the await analysis), with E0904.
stdout_keep_match=error\[E0[0-9]+\]|internal compiler error|is not supported inside an .io\.async. body that awaits
timeout=120
