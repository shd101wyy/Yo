# plans/reference/TRAIT_COHERENCE.md rule 3, the other registration order: a concrete impl
# of a trait for a type a registered blanket impl already covers is an error.
stdout_keep_match=already implemented for type|covers
timeout=120
