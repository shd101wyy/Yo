# Bare `unsafe(...)` in safe code (port of unsafe-gate.test.ts): calling
# `unsafe(...)` inside a function BODY without pragma(Pragma.AllowUnsafe) must
# FAIL under BOTH compilers. Without this gate, wrapping any pointer op in
# `unsafe(...)` would bypass the deref/arith gates (plans/reference/MEMORY_SAFETY.md
# Phase C).
#
# Blocked until 2026-08-12 for the same reason as its addr-of sibling — the
# privilege check throws inside main's body, the def-eval swallow ate it, and
# the self-hosted compiler emitted a runnable no-op binary. Now caught by the
# `__yo_user_main` marker gate.
# stdout_keep_match (P2.5 step 15): this case previously used stdout=ignore,
# which ANY failure satisfied. The kept substring is the probe-verified
# SHARED diagnostic - both compilers must emit it, so the case asserts THE
# failure, not just A failure.
stdout_keep_match='unsafe\(\.\.\.\)' is not available in safe code
timeout=120
