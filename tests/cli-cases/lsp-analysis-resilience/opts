# Analysis state survives errors (issues/fixed/lsp-analysis-state-lost-on-parse-and-eval-errors.md):
# an EVALUATION error keeps the parsed program and the ExprInfo recorded up to
# the error (hover still typed, outline complete, signature help answers); a
# PARSE error keeps the LAST PARSED outcome (outline, hover and completion keep
# answering while the diagnostic reports the parse error); a fixed text replaces
# it. Before: any parse error emptied the outline and silenced hover/completion,
# and any evaluation error dropped every type.
timeout=240
