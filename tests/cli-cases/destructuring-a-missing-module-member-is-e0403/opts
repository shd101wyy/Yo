# Destructuring a name a module does not export is E0403 and lists what it
# exports; it printed the module's whole struct type as the receiver.
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
