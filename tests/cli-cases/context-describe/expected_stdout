$ yo context --path src alpha
Building the context index for <PROJ>/src (key 7d02a5fc7721)...
  [1/2] indexed
  [2/2] indexed
src/alpha — Alpha: growable sequence helpers for the demo corpus.

  Point  type  Point
  add    fn  fn(a : i32, b : i32) -> i32
rc=0
$ yo context --path src alpha add
src/alpha — add

fn  fn(a : i32, b : i32) -> i32

### `add`

```rust
fn(a : i32, b : i32) -> i32
```

Adds two numbers.

**Parameters:**

- `a` : `i32`
- `b` : `i32`

**Returns:** `i32`
rc=0
$ yo context --path src alpha Point
src/alpha — Point

type  Point

### `Point`

*struct*

```rust
Point
```

A point in the plane.

**Fields:**

- `x` : `i32`
- `y` : `i32`

---
rc=0
