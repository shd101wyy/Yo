{ println } :: import("std/fmt");
main :: (fn() -> unit)({
  c := runtime(true);
  v : i32;
  cond(
    c => {
      v = i32(1);
    },
    true => {
      println(v.to_string());
    }
  );
});
export(main);
