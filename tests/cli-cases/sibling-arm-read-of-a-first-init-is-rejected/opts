# An arm reading a variable that only a sibling arm initialized is an uninitialized
# read: every arm starts from the state before the branch (Phase 5.6).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
