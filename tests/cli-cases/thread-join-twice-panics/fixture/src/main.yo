{ Thread } :: import("std/thread");
{ println } :: import("std/fmt");
main :: (fn() -> unit)({
  t := Thread.spawn(io => ());
  t.join();
  println(`joined once`);
  // Joining twice used to be undefined behaviour; it is a panic now.
  t.join();
  println(`joined twice?!`);
});
export(main);
