{ println } :: import("std/fmt");

main :: (fn() -> unit)({
  arr := Array(i32, usize(4)).fill(i32(7));
  println(`in-bounds=${arr(usize(3))}`);
  oob := arr(usize(10));
  println(`never=${oob}`);
});
export(main);
