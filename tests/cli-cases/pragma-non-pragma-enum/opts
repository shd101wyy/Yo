# pragma(...) argument validation (port of src/tests/pragma-validation.test.ts):
# the compile must FAIL (rc=1) under BOTH compilers. The validation message
# itself goes to stderr (not diffed); the rc parity is the assertion — the
# self-hosted compiler accepted this file silently before the Phase-G port.
timeout=120
