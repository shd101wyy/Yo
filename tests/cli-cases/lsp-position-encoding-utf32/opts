# UTF-32 negotiation (issues/fixed/lsp-initialize-discards-the-clients-params.md):
# the client lists `utf-32` in `general.positionEncodings`, the server answers
# `positionEncoding: utf-32` — its native rune unit — and every column on the
# emoji line is the rune column (compare lsp-position-encoding-utf16, same
# document and requests, columns shifted by one).
timeout=240
