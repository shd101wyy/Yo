# References/rename follow the BINDING, not the spelling
# (issues/fixed/lsp-references-and-rename-match-by-spelling.md): renaming the
# local `x` leaves the struct field `x`, the label `x :` and the member access
# `p.x` alone. Member and label atoms are never variable references: hover on
# `p.x` shows the field's type, definition there lands on the FIELD declaration
# (issues/fixed/lsp-member-definition-targets.md) and rename answers nothing
# (issues/fixed/lsp-member-and-label-atoms-resolve-to-same-named-locals.md).
# Also pins folding's duplicate-range fix
# (issues/fixed/lsp-folding-emits-duplicate-ranges.md).
timeout=240
