# E0007 with TWO comma-separated items, one of them not a field: the author
# may have meant a block (`;`) or a record with a bad field, so the diagnostic
# carries the message and NO repair — `yo fix` never picks between two fixes.
# The single-expression shape does carry one (fix-inserts-block-semicolon).
