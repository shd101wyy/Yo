# plans/reference/TRAIT_COHERENCE.md rule 3: a blanket impl covering a type that already
# has a concrete impl of the trait is an error (no specialization; the concrete impl used to
# win silently — issues/fixed/an-overlapping-blanket-trait-impl-is-silently-dead.md).
stdout_keep_match=already implemented for type|covers
timeout=120
