# Safe mode Phase 1 (H2): str.bytes(i) at i == len() aborts with the
# index-out-of-bounds diagnostic (it used to read past the static bytes).
# Recorded by hand.
stdout_keep_match=index out of bounds: 3 not in \[0, 3\)
timeout=300
