{ println } :: import("std/fmt");

byte_at :: (fn(s : str, i : usize) -> u8)({
  return(s.bytes(i));
});

main :: (fn() -> unit)({
  s := "abc";
  println(`in-range=${byte_at(s, usize(2))}`);
  b := byte_at(s, s.len());
  println(`never=${b}`);
});
export(main);
