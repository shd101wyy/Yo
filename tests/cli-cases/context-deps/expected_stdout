$ yo context --path src --deps mylib
Building the context index for <PROJ>/src (key f6570b04e0b9)...
  [1/2] indexed
  [2/2] indexed
Building the context index for <PROJ>/mylib (key 33e199051dbb)...
  [1/1] indexed
mylib/mylib — Mylib: a tiny path dependency.

  double  fn  fn(x : i32) -> i32
rc=0
$ yo context --path src --deps mylib double
mylib/mylib — double

fn  fn(x : i32) -> i32

Doubles a number.

### `double`

```rust
fn(x : i32) -> i32
```

Doubles a number.

**Parameters:**

- `x` : `i32`

**Returns:** `i32`


rc=0
$ yo context --path src --deps --search double
mylib/mylib  double  fn  fn(x : i32) -> i32  (400)
— 1 hit(s)
rc=0
