# `f(i32(1);` inside a body: the `;` used to turn the argument list into a
# block that swallowed the next statement, with the body's `}` accepted inside
# the open `(` — and check passed
# (issues/fixed/parser-accepts-an-unclosed-call-paren.md). Now E0002 at the `;`.
