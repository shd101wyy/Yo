error: Field "r" of a reference type cannot hold ctl(msg : String) -> i32: it is (or contains) a `ctl` handler, which unwinds into the frame that installed it. A reference object outlives that frame, so a later call through the object would unwind into a frame that no longer exists. Pass the handler as an argument instead, or keep it in a value struct.
--> main.yo:4:22
error: check: 1 file(s) failed evaluator coverage
