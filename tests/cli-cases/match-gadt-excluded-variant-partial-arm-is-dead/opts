# issues/fixed/match-demands-a-missing-case-of-a-gadt-excluded-variant.md: over
# `Value(i32)`, `.BoolVal(true)` made the checker demand `.BoolVal(false)`, a
# value that type cannot hold (E0607). The arm is dead instead (E0608).
stdout_keep_match=(error\[E0[0-9]+\]|Unreachable match arm: [^ ]+|Missing case: [^ ]+)
