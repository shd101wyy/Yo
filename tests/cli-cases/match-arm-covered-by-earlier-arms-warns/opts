# plans/TYPE_SYSTEM_SOUNDNESS.md Phase 4.5: an arm the earlier arms cover only
# TOGETHER is a warning (the warnings channel), not an error; `check` still
# passes. Two cases: `.Some(_)` after both bools, and `(5..=9)` after two
# ranges that cover it between them.
stdout_keep_match=(warning: unreachable match arm: [^—]*)
