# `yo add ./mylib` declares a path dependency in yo.toml IN PLACE (the comment
# in the fixture's [dependencies] table survives), then `build run` imports
# it by name — the §1.1 reproducer through the CLI that writes the manifest.
timeout=600
stdout_keep=^(Added|Done|Building|12$)
