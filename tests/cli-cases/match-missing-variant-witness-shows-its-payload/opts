# plans/TYPE_SYSTEM_SOUNDNESS.md Phase 4.5: a missing case renders every payload
# field, `.Circle(_)`, where a bare `.Circle` read as "no arm names it".
stdout_keep_match=(error\[E0[0-9]+\]|Missing case: [^ ]+)
