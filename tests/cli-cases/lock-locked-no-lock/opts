# `--locked` with no yo.lock at all: there is nothing to obey, so the run
# fails (rc 1) instead of writing a first lock — CI must not mint one.
timeout=600
stdout_keep_match=Error: --locked: there is no yo\.lock to obey
