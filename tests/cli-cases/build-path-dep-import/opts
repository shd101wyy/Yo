# The audit's headline reproducer (plans/BUILD_AND_DEPENDENCY_SYSTEM_REDESIGN.md
# §1.1): a path dependency declared in yo.toml, imported by NAME from the
# program — `import("mylib")` (the dependency's `[modules] default`) and the
# sub-module form `import("mylib/extra")` (a sibling of the default root). The
# runner resolves the manifest closure (src/manifest.yo) and hands the mapping
# to the child compile as `--imports`; the evaluator's resolver rule 0
# consumes it. The program's own output is the assertion.
timeout=600
stdout_keep=^(Building|12$|15$)
