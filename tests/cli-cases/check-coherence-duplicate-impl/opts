# plans/reference/TRAIT_COHERENCE.md rule 1: two impls of one trait for one type in one
# module are an error naming the first impl (it used to register both; dispatch took the
# first, so the second was dead code — issues/fixed/yo-self-missing-duplicate-impl-checks.md).
stdout_keep_match=already implemented for type|first impl
timeout=120
