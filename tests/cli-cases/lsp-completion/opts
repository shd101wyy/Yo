# Slice-4 gate (plans/archive/P4_LSP.md): textDocument/completion in every mode
# against one opened document — import-path completion (`import("std/coll`),
# struct-value dot (`p.` → fields + inherent method), type dot (`Color.` →
# variants with payload snippets), enum dot-prefix (`.` in a match branch →
# `.Red`/`.Green`/`.Blue` from the subject's type), generic-impl methods
# (`list.` on ArrayList(i32)), and identifier-prefix completion (`Poi` →
# Point + env/keyword candidates). stdin carries the framed JSON-RPC bytes;
# the doc lives at a virtual uri so the golden is machine-independent. The
# timeout covers the prelude evaluation the first analysis pays.
timeout=240
