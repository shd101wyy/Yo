{ assert } :: import("std/assert");
test("a body that references a name that does not exist", {
  // Without --test-bodies this file reports "evaluator OK": the test-body
  // trial swallows every error, so only the ~30-minute suite finds it.
  assert(totally_undefined_name_zzz == i32(1), "unreachable");
});
