# A ctl handler resuming with the wrong type (`return(String)` for an `i32`
# resume) is reported at `check`, not swallowed into an abort() stub
# (issues/fixed/closure-body-type-errors-are-swallowed-into-a-runtime-abort.md, repro 2).
stdout_keep_match=error\[E0601\]|--> main\.yo:[0-9]+:[0-9]+
timeout=120
