# plans/reference/TRAIT_COHERENCE.md rules 1-2 across modules: `lib.yo` implements `Foo`
# for `P`, and `main.yo` implements it again for the imported `P`.
stdout_keep_match=already implemented for type|first impl
timeout=120
