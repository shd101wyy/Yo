# The public-signature raw-pointer lint (port of src/public-safe-report.ts):
# both formats must be BYTE-IDENTICAL between compilers. The fixture has one
# leaking public fn (leaky: parameter + return findings), three contract-
# exempt names (as_ptr, read_ptr, extend_from_ptr), an underscore-private fn,
# and a clean public fn. The lint is informational: rc=0 even with findings.
timeout=120
