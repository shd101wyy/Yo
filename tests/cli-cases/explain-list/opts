# Pins the registry's code set: `--list` names every code with its title;
# E0610 (no matching call) is new; E0904 (await placement) was retired because
# its message has no raise site, so explaining it is the unknown-code path
# (rc=1 ends the case there — the harness stops at the first non-zero rc).
