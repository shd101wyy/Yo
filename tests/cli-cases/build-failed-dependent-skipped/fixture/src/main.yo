{ println } :: import("std/fmt");

// A deliberate type error: the artifact cannot compile, so the `run` node that
// depends on it must not execute.
main :: (fn() -> unit)({
  println(i32(1) + "not a number");
});

export(main);
