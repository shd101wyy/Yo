error[E0908]: Cannot write `d.s`: `d` is a `match` or `for` binding, which borrows its value, and the old value holds data (String) that its owner still holds. Writing it would release that data. To change it, copy it into a local first (`q := d;`) and write the copy.
--> main.yo:7:8
error: check: 1 file(s) failed evaluator coverage
