# A `__yo_panic` reached while a comptime function body executes is a
# compile error carrying its message (issues/fixed/a-comptime-panic-is-not-a-compile-error.md;
# plans/SAFE_MODE.md §14 R2). Before the fix `check` passed (rc 0) and
# `compile` failed in clang with `use of undeclared identifier 'LIMIT'`.
# Recorded by hand: the recording binary must carry the fix.
stdout_keep_match=compile-time panic: Called comptime_unwrap on a None value
timeout=300
