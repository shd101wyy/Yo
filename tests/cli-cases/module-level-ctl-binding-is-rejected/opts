# A module-level binding of a control-bound type, in the typed `(g : T) = init` form,
# is rejected like the `:=` form (Phase 5.5).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+
timeout=120
