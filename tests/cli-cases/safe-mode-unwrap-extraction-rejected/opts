# Safe-mode D7: the EXTRACTION form `Option(i32).unwrap` (binding the method
# as a callable value) is the bypass the ban must also close - gated in the
# TypeVal arm of property_access. Recorded by hand: the recording binary
# must carry the gate.
stdout_keep_match=discards the failure case
timeout=300
