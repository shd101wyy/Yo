# io.await in an unsplittable position (port of src/tests/async-await-position-gate.test.ts):
# the compile must FAIL (rc=1) under BOTH compilers — this failure used to be
# SILENT (a C comment + NULL await_future_N deref at runtime). The diagnostic
# text goes to stderr (not diffed); the rc parity is the assertion.
timeout=120
