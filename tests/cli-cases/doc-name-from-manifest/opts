# `yo doc` titled the site after the DIRECTORY for any project whose name lives
# only in `yo.toml` — which is every project `yo init` creates. The inference
# chain predated the manifest and consulted package.json, then a regex over
# build.yo, then the directory
# (issues/fixed/yo-doc-infers-the-project-name-from-package-json.md).
#
# The fixture's directory is the sandbox's `proj`, so "acme" can only have come
# from `[package] name`. `--format markdown` so the assertion lands in a small
# README.md a re-recorder can eyeball rather than in a 200 KB HTML page.
timeout=300
