# UTF-16 wire columns (the LSP default, plans/archive/P4_LSP.md quality items):
# the client offers no `general.positionEncodings`, so the server answers
# `positionEncoding: utf-16` and converts every column. Line 2 of the document
# carries an astral-plane rune (😀 = two UTF-16 units) BEFORE the identifiers, so
# a rune column and a UTF-16 column differ by one there: hover at the UTF-16
# column of `y` answers, hover at its rune column does not; rename/references/
# definition/diagnostics ranges on that line are shifted by one unit
# (issues/fixed/lsp-emits-rune-columns-where-the-protocol-says-utf16.md).
timeout=240
