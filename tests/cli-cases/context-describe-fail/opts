# A module-scoped item miss exits 1 with did-you-mean names from that module
# on stderr. (The bare-name miss is context-not-found: a run stops at the
# first nonzero rc, so a second failing command here never ran.)
env=YO_CONTEXT_CACHE=<PROJ>/cache
