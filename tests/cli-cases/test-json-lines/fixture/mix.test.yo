{ assert } :: import("std/assert");
test("passes and prints", {
  println("hello from the passing test");
  assert((i32(1) + i32(1)) == i32(2));
});
test("fails on purpose", {
  assert((i32(1) + i32(1)) == i32(3));
});
