# `io.await` on a JoinHandle is E0602 at the argument, with the trait and the
# parameter printed as written (Phase 4.1/4.4).
stdout_keep_match=error(\[E[0-9]+\])?: .*|--> main\.yo:[0-9]+:[0-9]+|- (Parameter|Argument).*
timeout=120
