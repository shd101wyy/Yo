# `sizeof(*(i32))` in a BODY of a safe file must be reported as the safe-code
# rejection by `compile`. Before the id-0 fix, `sizeof` adopted the environment
# of the prelude node that shared the error sentinel's id, and `compile`
# reported `Variable "println" not found` on the NEXT line instead
# (issues/fixed/compile-reports-a-derived-error-instead-of-the-safe-code-raw-pointer-rejection.md).
stdout_keep_match=Raw pointer types \('\*\(i32\)'\) are not available in safe code
timeout=120
