// Imported ONLY by z_hermetic.test.yo — mirror of a_helper.yo so the pin
// holds regardless of which test file the runner reaches first.
(g_z_helper_state : i32) = i32(100);
z_bump :: (fn() -> i32)({
  g_z_helper_state = (g_z_helper_state + i32(1));
  g_z_helper_state
});
export(z_bump);
