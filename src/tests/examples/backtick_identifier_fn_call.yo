extern 
  add : (fn(x: i32, y: i32)-> i32)
;
add_2 :: (fn(x: i32, y: i32)-> i32)
  add(x, y);

add_3 :: (fn(x: i32, y: i32, z: i32)-> i32)
  add(x, add(y, z));


main :: (fn() -> unit) {
  x := add_2(1, 2);
  x2 := (1 `add_2` 2);

  // x := add_3(1, 2, 3);
  // x := (1 `add_3` 2, 3);
  
  x3 := ((1 `add_2` 2) `add_2` 3);
}