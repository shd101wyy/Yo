main :: (()-> unit) {
  SomeStruct :: ((compt(T): Type) -> compt(Type))
    struct(x: T)
  ;


  (x: SomeStruct(i32)) = SomeStruct(i32)(1);
  (x2: SomeStruct(i32)) = _(1);
  (y: SomeStruct(i32)) = x;


  AnotherStruct :: ((compt(T): Type) -> compt(Type))
    struct(x: T)
  ;
  // (z: AnotherStruct(i32)) = x; // Should give error as the struct is nominal type in Yo

};