Box :: ref struct(
  (*) : i32

  // dispose :: (fn(self : Self) -> unit) {
  //   printf("Dropping Box with value %d\n", self.(*));
  // }
);

// tuple
main :: ((fn() -> unit) {
  x := Box(3);
  y := (x,); // _yo65d85c3c_temp_5
  // (_yo65d85c3c_temp_5.___drop)()
});

test2 :: ((fn() -> Tuple(Box)) {
  x := Box(3);
  y := (x,);
  y
});

test2_2 :: ((fn() -> Tuple(Box)) {
  x := Box(3);
  y := (x,);
  return y;
});

test3 :: ((fn() -> Tuple(Box)) {
  x := (Box(3),);
  x
});

// export main;