Box :: (fn(compt(V) : Type) -> compt(Type))
  ref struct(
    (*) : V
  )
;
box :: (fn(forall(V : Type), value : V) -> Box(V))
  Box(V)(value)
;

main :: (fn() -> int) {
  x := box(3);
  y := Box(i32)(4);
  return 0;
};

export main;