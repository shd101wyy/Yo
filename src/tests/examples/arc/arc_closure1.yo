open import "std/libc/stdio.yo";

main :: (fn() -> unit) {
  x := 1;
  closure := ((fn(y : i32) => i32) {
    x = (x + y);
    return x;
  });

  closure(1); // 2
  closure(1); // 3

  result := closure(2); // 5
  printf("Final result: %d\n", result);
};

export main;