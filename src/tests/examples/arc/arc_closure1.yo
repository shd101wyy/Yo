open import "std";

main :: (fn() -> i32) {
  x := 1;
  closure := ((fn(y : i32) => i32) {
    x = (x + y);
    return x;
  });

  closure(1); // 2
  closure(1); // 3

  return closure(2); // 5
};

export main;