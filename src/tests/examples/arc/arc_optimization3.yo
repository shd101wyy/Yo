extern "Yo",
  some_cond : (fn() -> bool)
;

Box :: object(
  (*) : i32
);

BoxBox :: struct(
  (*) : Box
);

Color :: enum
  Red,
  Green,
  Blue
;

test1 :: ((fn(color : Color) -> i32) {
  x := Box(3);
  match(color,
    .Red => {
      y := BoxBox(x);
    },
    .Green => {
      y := BoxBox(x);
    },
    .Blue => {
      y := BoxBox(x);
    }
  );
  return 0;
});

test2 :: ((fn(color : Color) -> i32) {
  x := Box(3);
  match(color,
    .Red => {
      y := BoxBox(x);
    },
    .Green => {
      ();
    },
    .Blue => {
      y := BoxBox(x);
    }
  );
  return 0;
});


test3 :: ((fn(color : Color) -> i32) {
  x := Box(3);
  match(color,
    .Red => {
      ();
    },
    .Green => {
      ();
    },
    .Blue => {
      ();
    }
  );
  return 0;
});

export test1, test2, test3;
