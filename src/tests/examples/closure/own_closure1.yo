let test = (fn: [=](y: i32)-> unit, y: i32)-> unit {
  fn(y);
}

let main = ()-> {
  var x = 1;
  var func = [=](y: i32)-> {
    x = x + y;
  };
  test(func, 2);
  // func.call(2); // Compiler error
  // x should still be 1.  
}
