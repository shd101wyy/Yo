extern
  add:
    (a: i32, b: i32) -> i32;

x := 1;

test :: ((a: i32)-> i32) {
  y := 2;

  // another _test can access `x`, but not `y`.
  // to access `y`, use closure.
  compt_expect_error(
  another_test :: (()-> i32)
    y `add` x
  );
  
  a `add` x
}