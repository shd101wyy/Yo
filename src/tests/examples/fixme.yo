open import "std/collections/hash_map.yo";
HM :: HashMap(i32, i32);

main :: (fn() -> unit)({
  map := HashMap(i32, i32).new();

  {
    b := box(123);
    {
      ();
      b
    };
  };
  return ();
});

export main;


/*
Data :: object(
  (*) : i32,
  len :: (fn(self : Self) -> i32)(self.*)
);
// TODO: pass it as fn arg.

*/