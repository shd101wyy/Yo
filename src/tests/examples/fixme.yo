main :: (() -> unit) {
  x := 12;
  y := 12.3;

  MyTuple :: Tuple(x: i32, i32, i32);
  MyArray :: Array(i32, 3);

  {
    // Create an array of integers with 3 elements
    mut(arr) : Array(i32, 3);
    arr = [1, 2, 3];
    arr = array(1, 2, 3);
  };

  {
    // Or create an array directly
    x := 2;
    arr := [1, x, 3];
  };

  {
    (arr: Array(i32, 3)) = [1, 2, 3];
    elem := arr(0);
  };

  /*

  x := arr(0); // Calling on the `arr` it self is equalvalent to
  x := arr.subscript(0); // calling the subscript method

  */
  ()
};
