open import "std";
open import "std/libc/stdio";

add :: ((compt(x) : i32, compt(y) : i32) -> 
  (compt(ret) : i32)
)
{
  return x + y;
};

/*
test :: ((compt(x) : i32, compt(y) : i32) ->  ((output : compt(i32)) `with` {
 pre  : (x < y),
 post : output < 100
})) 
  x + y
;
*/

main :: (() -> int) {
  z :: add(3, 4);
  return 0;
};