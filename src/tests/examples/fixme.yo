use_option :: (fn(o : Option(Box(i32))) -> unit) {
  p := o;
  match(p,
    .Some(v) => (),
    .None => ()
  );
};

main :: (fn() -> unit) {
  o := Option(Box(i32)).Some(Box(i32)(12));
  use_option(o);
};

export main;