open import "std";

// Mutal recursion
compt(fn1) : ((x : i32) -> i32);
compt(fn2) : ((x : i32) -> i32);

fn1 = ((x : i32) -> i32) {
  return fn2(x - 1);
};

fn2 = ((x : i32) -> i32) {
  return fn1(x - 1);
};