open import "std/libc/stdio.yo";

say :: (fn(s : *(u8)) -> i32) {
  printf("%s\n", s);
  return 16;
};

main :: (fn() -> i32) {
  t := spawn say("world"); // say("world") runs in parallel thread
  say("hello");
  
  // result := __yo_thread_wait(t);
  // printf("result: %d\n", result);
  // return result;

  return 0;
};

export main;