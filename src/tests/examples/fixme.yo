Id :: module
  Self : Type,
  Output ?= Self,
  id : (fn(value : Self) -> Output)
;

I32Id :: Id(
  Self : i32,
  id : ((value) -> value)
  // , Output : Self // FIXME: Expected to still work without this line.
);

main :: (fn() -> unit) {
  x := 12;
  y := I32Id.id(x);
};

export main;