Id :: module(
  where(Self <: Copy),
  id : (fn(x : Self) -> Self)
);

use_id :: (
  fn(
    compt(T) : Type, 
    x : T, 
    where(T <: Id)
  ) -> T) {
  y := x.id();
  z := (T <: Id).id(x);
  return z;
};

main :: (fn() -> unit) {
  compt_expect_error((i32 <: Id).id(5));
};