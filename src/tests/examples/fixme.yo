id_func :: 
  ((forall(@(T): Free),
    x : T)-> T) {
  (xx : T) = x; // Use `T` inside the function body
  xxx := x;

  mut(xxxx) := xxx;
  // (mut(xxxx) : typeof(x)) = xxx;
  xxxx = xx;
  xxx
};

x3 := id_func(12);