// Builtin function to set the type module.  
// This function should only be called once.  
// Calling more than once should throw error.  
__yo_set_type_module i32,
  id :: ((fn(self : Self) -> Self) {
    return self;
  })
;

/*
main :: (fn() -> unit) {
  x := 12;
  y := i32.id(x);
  z := x.id();
};
*/