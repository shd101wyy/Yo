/*
swap :: (( x : *!(i32), y : *!(i32) ) -> unit) {
  temp := x.*;
  x.* = y.*;
  y.* = temp;
};

main :: (() -> unit) {
  mut(x) := 1;
  mut(y) := 2;
  swap(*!(x), *!(y));
};
*/

extern
  add :
    (x : i32, y : i32) -> i32
;

Point :: struct
  x : i32,
  y : i32
;

main :: (() -> i32) {
  x := {
    y := 12;
    y
  };
  y := {
    x := 34;
    x;
  };
  x
};

export
  main
;