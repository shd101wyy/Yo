use_box :: (fn(own(x) : Box(i32)) -> i32) {
  return x.*;
};

main :: (fn() -> unit) {
  x := Box(i32)(42);
  result := use_box(x); // `x` is consumed here, and cannot be used afterwards
  y := x; 
  return ();
};

export main;