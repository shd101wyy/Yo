/**
 *
 * Slice type
 *
 */

S :: Slice(i32);

/*
extern "Yo",
  __yo_slice_ref :
    (forall(@(T): Type, @(size) : usize), 
      arr: &(Array(T, size)),
      start : usize,
      end: usize
    ) -> Slice(T)
;

main :: (() -> unit) {
  arr := [1, 2, 3, 4];
  arr_slice := &(__yo_slice_ref(&(arr), 1, 3));
};
*/