Id :: module(
  id : (fn(self : Self) -> Self)
);

test :: (fn(compt(T) := Impl(Id), x : T) -> T) {
  y := typeof(x).Id.id(x);
  return y.id();
};

test2 :: (fn((compt(T) : Type) = Impl(SomeId : Id), x : T) -> T) {
  y := typeof(x).SomeId.id(x);
  return y.id();
};

