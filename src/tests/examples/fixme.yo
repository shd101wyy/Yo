extern "Yo",
  some_cond : (fn() -> boolean),
  (<) : (fn(lhs: i32, rhs: i32) -> boolean),
  (+) : (fn(lhs: i32, rhs: i32) -> i32)
;

Box :: ref struct(
  (*) : i32
);

BoxBox :: struct(
  (*) : Box
);

test1 :: ((fn() -> i32) {
  x := Box(3);
  i := 0;
  while (<)(i, 100), i = (+)(i, 1), {
    y := BoxBox(x);
  };
  return 0;
});

export test1;
