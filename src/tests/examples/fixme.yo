main :: (fn() -> i32) {
  x := 1;
  closure := ((fn(v : i32) => i32) {
    y := x;
    {
      z := x;
      x := 12;
      p := x;
    };
    return x;
  });

  closure(1);
  closure(1);

  return closure(2);
};

export main;