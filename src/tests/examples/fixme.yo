(compt(LinearStruct) : Linear) = struct(
  x : i32,
  y : i32
);

main :: (() -> i32) {
  // This should work - LinearStruct gets auto-generated drop
  linear_struct_1 := LinearStruct(1, 2);
  linear_struct_2 := LinearStruct(1, 2);
  // drop(linear_struct2); // This line should be automatically inserted
  // drop(linear_struct1); // This line should be automatically inserted
  return 0;
};

export main;
