open import "std";

compt_factorial :: ((@(x) : compt_int) -> @(compt_int))
  cond
    (x @== 0) => 1,
    true =>
      x @* recur(x @- 1)
;

// a :: compt_factorial(5);

run_compt_factorial :: ((@(x) : compt_int) -> @(compt_int)) {
  x :: compt_factorial(x);
  x
}
;
