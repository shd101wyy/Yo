open import "std/collections/array_list";
open import "std/string";

main :: (fn() -> unit)({
  bytes := ArrayList(u8).new();
  bytes.push(u8(72));
  bytes.push(u8(101));
  str := String.from_bytes(bytes);
  result_bytes := str.as_bytes();
});

export main;
