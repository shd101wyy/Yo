/**
 * Macro
 *
 */
open import "std";

x := 16;
y := 18;

add :: ((quote(x) : Expr, quote(rest): ExprList) -> unquote(Expr))
  quote add(unquote(x), unquote_splicing(rest))
;

expanded :: macro_expand quote(add(1, 2, x, y));