/*
swap :: (( x : *!(i32), y : *!(i32) ) -> unit) {
  temp := x.*;
  x.* = y.*;
  y.* = temp;
};

main :: (() -> unit) {
  mut(x) := 1;
  mut(y) := 2;
  swap(*!(x), *!(y));
};
*/

extern
  add :
    (x : i32, y : i32) -> i32
;

Point :: struct
  x : i32,
  y : i32
;

main :: (() -> i32) {
  p := Point(y: 3, x: 4);
  0
};

export
  main
;