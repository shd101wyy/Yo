Point :: struct
  x : i32,
  y : i32
;

id :: ((x : i32) -> i32)
  x
;

main :: (() -> c_int) {
  x := 12;
  y := id(13);
  z := id(14);

  p1 := Point(3, 4);
  p2 := Point(y : 5, x: 6);
  
  0
};

// `export` here is required. Otherwise no functions will be compiled.
export
  main;
