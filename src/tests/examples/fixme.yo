// 
open import "std/libc/stdio";
// open import "std/collections";

main :: (fn() -> unit) {
  (x : [u8]) = "你好，世界!";
  printf("bytes length: %zu %d %d\n", x.len(), x(0), x(15));

  y := "你好，世界!";
  printf("bytes length: %zu %d %d\n", y.len(), y(0), y(15));
};

export main;