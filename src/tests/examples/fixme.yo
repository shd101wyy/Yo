open import "std";

Point :: struct(x : i32, y : i32);

main :: (() -> unit) {
  x :: quote(some_atom);
  flag :: x.is_atom();

  p :: Point(3, 4);
  p_x := p.x;
  p_y := p.y;

  arr := [1, 2, 3];
  len :: arr.length;

  slice := *(arr(1:2));
  slice_len := slice.length;
};