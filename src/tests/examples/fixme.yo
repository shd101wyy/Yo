Gc :: import "std/gc";

test_fn :: (fn() -> unit) {
  {
    flag := (Gc.tracked_count() == 0);
    assert(flag, "Expected 0 tracked objects at start");
  };
  {
    assert((Gc.tracked_count() == 0), "Expected 0 tracked objects at start");
  };
};

test "Test Gc tracked count", {
  test_fn();
};