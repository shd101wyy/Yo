extern "C",
  @(malloc) :
    ()-> ^(i32)
;

test :: (()-> unit) {
  mut(x) := malloc();
  consume(x);
  compt_expect_error({
  y := (
    x = malloc());
    consume(x);
  }, "x is already consumed, so cannot mutate again");
};