/*
lst :: compt_list(1, 2, 3);
first :: lst.first();
rest :: lst.rest();
length :: lst.length();

appended :: compt_list(1, 2).append(ComptList(compt_int)(3, 4));
appended2 :: ComptList(compt_int)(1).append(compt_list(2, 3, 4));


// Type Info
ListType :: typeof(lst);

type_id      :: ListType.type_info.id;
tag          :: ListType.type_info.tag;
element_type :: ListType.type_info.element_type;
*/

/*
type_id      :: Type.id(ListType);
type_tag     :: Type.tag(ListType);
element_type :: Type.element_type(ListType);

is_struct :: (fn(compt(T) : Type) -> boolean) {
  
};
*/

TypeField :: struct(
  compt(label) : compt_string,
  compt(type)  : Type
);

X :: TypeField("Hi", i32);
A :: X.label;
/*
TypeInfo :: enum(
  Unit,
  Boolean,
  Usize,
  Isize,
  U8,
  I8,
  U16,
  I16,
  U32,
  I32,
  U64,
  I64,
  F32,
  F64,

  ComptInt,
  ComptFloat,
  ComptString,

  Char,
  Short,
  UShort,
  Int,
  UInt,
  Long,
  ULong,
  LongLong,
  ULongLong,
  LongDouble,

  Void,

  Type,

  Array(element_type : Type),
  Tuple,
  Struct,
  Enum,
  Union,
  Function,
  Closure,

  SomeType,

  Slice,

  Module,

  Ptr,

  Dyn,

  Expr,

  ComptList,

  Future
);
*/
