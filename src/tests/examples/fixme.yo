open import "std";

ID :: ((@(T): Type) -> @(Module))
  module
    Self := T,
    id : 
      (self : Self) -> Self
;

I32Id :: ID(i32)
  id :
    fn(self) -> {
      x := self;
      y := self;
      (x + y)
    }
  ;
