open import "std/libc/stdio";

main :: (fn() -> unit) {
  arr := [1, 2, 3];
  length1 := Array(i32, 3).len(&(arr));
  length2 := arr.len();
  printf("Array length: %u %u\n", length1, length2);

  s := arr(:);
  printf("Array slice length: %u\n", s.len());  
};

export main;