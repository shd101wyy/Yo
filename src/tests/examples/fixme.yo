open import "std";
open import "std/libc/stdio";
open import "std/collections";

main :: (fn() -> unit) {
  (x : *([u8])) = ("你好，世界!");
  // x := "你好，世界!";
  printf("bytes length: %zu\n", x.length);
};

export main;