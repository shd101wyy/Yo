use_cb :: (fn(v : i32, cb : (fn(u : i32) -> i32)) -> i32) {
  return cb(v);
};

main :: (fn() -> unit) {
  x := use_cb(10, (u)-> (u + 1));
  assert(x == 11);

  y := use_cb(20, (u)-> (u * 2));
  assert(y == 40);
};

export main;