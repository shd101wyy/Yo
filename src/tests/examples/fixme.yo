open import "std/libc/stdio.yo";

Box :: object
  (*) : i32,
  
  dispose :: ((fn(self : Self)-> unit) {
    printf("Disposing Box with value %d\n", self.*);
  })
;

main :: (fn() -> i32) {
  x := Box(3);
  return x.*;
};

export main;