open import "std";
open import "std/libc/stdio.yo";

main :: (fn() -> unit) {
  ch := chan(i32);
  
  async {
    x := 1;
    ch <- (x + 2);
  };
  
  value := <-(ch).unwrap();
  printf("value=%d\n", value);
};

export main;
