/*
open import "std/string";

main :: (fn() -> unit) {
  str := String.new();  
  result_bytes := str.as_bytes();
};

export main;
*/

// Test implicit return of RC field
Foo :: object(
  data: Box(i32),

  new :: (fn(val: i32) -> Self)(
    Self(data: box(val))
  ),

  // FIXME: This doesn't
  // This should generate a dup call
  get_data :: (fn(self: Self) -> Box(i32))(
    self.data
  )

  // THIS WORKS:
  // get_data :: (fn(self: Self) -> Box(i32))({
  //   return self.data;
  // })
);

main :: (fn() -> unit) {
  foo := Foo.new(42);
  d := foo.get_data();
};

export main;