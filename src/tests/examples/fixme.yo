test_fn :: (fn(errno : i32) -> i32) {
  return errno;
};

main :: (fn() -> unit) {
  errno := 2; // This local variable matches C reserved `errno`.
  cond(
    (errno == 2) => "yes",
    true => "no"
  );
  assert(test_fn(errno) == 2);
};

export main;
