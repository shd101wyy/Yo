main :: (fn() -> unit) {
  // Get the array length
  arr := [1, 2, 3];
  length :: arr.length;

  // define our own function:
  array_length :: (fn(forall(T: Type, n: usize), arr: [T; n]) -> (usize))
    n
  ;
  length2 := array_length(arr);
};
