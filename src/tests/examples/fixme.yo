use_box :: (fn(own(x) : Box(i32)) -> i32) {
  return x.*;
};

main :: (fn() -> unit) {
  x := Box(i32)(42);
  y := x;

  Var.print_info(x);
  f :: (Var.is_owning_the_gc_value(x)); // true
  Var.print_info(y);
  f2 :: (Var.has_other_aliases(y)); // true
};

export main;