Worker :: (fn(compt(A) : Type, compt(B) : Type) -> compt(Type)) {
  Child :: recur(B, A);

  object(
    ptr : *(void),
    _test :: (fn(v : *(void)) -> unit)({
      Self(v);
      Child(v);
    })
  )
};
