open import "std";

main :: (fn() -> unit) {
  mut(x) := 12;
  closure := ((fn(y : i32) => i32) {
    x = (x + y);
    x
  });
  // It's the same as 
  // closure := Closure(FnOnce(y : i32) -> i32, _) {
  //   x = (x + y);
  //   x
  // };

  closure(3);
  closure(4);
};