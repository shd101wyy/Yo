

main :: (fn() -> unit) {
  // I32Array :: Array(i32, 10);
  // length :: I32Array.length;
  x := 1;
  y := (x + 1);
  z := (x - 2);

  Add :: i32.Add;

  s :: Type.to_string(i32);

  {
    x := 12;
    p := &(x);
    q := &(y);
    flag := (p == q);
    // p2 := (p + __yo_compt_int_as(12, usize));
    // o3 := (p - __yo_compt_int_as(12, usize));

    z := p.cast(*(void));
  }
};
