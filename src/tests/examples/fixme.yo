Id :: module
  Self : Type,
  Output ?= Self,
  id : (fn(value : Self) -> Output)
;

I32Id :: Id(
  Self : i32,
  id : ((value) -> value)
);

main :: (fn() -> unit) {
  x := 12;
  y := I32Id.id(x);
};
