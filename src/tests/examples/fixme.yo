open import "std/libc/stdio";

MyModule :: impl {
  _some_cond :: (fn() -> boolean) {
    return true;
  };

  export
    some_cond : _some_cond
  ;
};

main :: (fn() -> unit) {
  flag := MyModule.some_cond();  
};

export main;
