implicit(ComptIntMethods) :: module {
  Self :: compt_int;

  (as) :: ((@(self): Self, @(Target): Type) -> @(Target))
    __yo_compt_int_as(self, Target)
  ;

  export
    as
  ;
};

x :: (3).as(f32);