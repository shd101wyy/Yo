SomeUnion :: union
  x : i32,
  y : boolean,

  new :: ((fn()-> Self) Self(x: 0)) 
;

main :: (fn() -> unit) {
  v1 := SomeUnion( x : 12 );
  v2 := SomeUnion( y : true );

  {
    a := v1.x;
  };

  {
    { x } := v1;
    { y: z } := v1;
  };
};
