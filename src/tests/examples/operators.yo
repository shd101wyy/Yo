open import "std";

extern
  add:
    fn(x: i32, y: i32)-> i32,
  sub:
    fn(x: i32, y: i32)-> i32
;

main :: (fn() -> unit) {
  Operators :: module {
    Self :: i32;

    ($+) :: (fn(x: i32, y: i32)-> i32)
      add(x, y)
    ;

    ($-) :: (fn(x: i32, y: i32)-> i32)
      sub(x, y)
    ;

    export
      Self, ($+), ($-)
    ;
  };

  Self :: Operators.Self;
  x := Operators.($+)(3, 4);
  x2 := 3.as(i32).($+)(4);
  x3 := (3.as(i32) $+ 4);

  x4 := Operators.($-)(3, 4);
  x5 := 3.as(i32).($-)(4);
  x6 := (3.as(i32) $- 4);
};
