ComptIntMethods :: impl {
  Self :: compt_int;

  (as) :: (fn(compt(self): Self, compt(Target): Type) -> compt(Target))
    __yo_compt_int_as(self, Target)
  ;

  export
    Self,
    as
  ;
};

compt_print(ComptIntMethods.Self);

Data :: (fn(compt(T): Type) -> compt(Type))
  struct(
    len: usize,

    return_i32 :: ((fn() -> usize)
      12
    ),

    new :: ((fn() -> usize) {
      len := usize(0);
      return len;
    }),

    new2 :: ((fn() -> usize)) {
      return Self.return_i32();
    }
  )
;
