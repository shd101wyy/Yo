Shape :: enum
  Circle(radius: i32),
  Rectangle(width: i32, height: i32),
  Triangle(base: i32, height: i32),

  new_circle :: (
    (fn(radius: i32) -> Self)
      .Circle(radius)
  ),
  id :: ((fn(self: Self)-> Self)
    self
  )
;
main :: (fn() -> unit) {
  c := Shape.new_circle(5);
  d := c.id();
};