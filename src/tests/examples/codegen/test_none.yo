Option :: ((@(T): Type)-> @(Type))
  enum
    Some(value: T),
    None
;

test_none :: (() -> i32) {
  p := Option(*(i32)).None;
  match p,
    .Some => p.value.*,
    .None => 42
};

export
  test_none
;
