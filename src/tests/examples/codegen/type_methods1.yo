Point :: struct
  x : i32,
  y : i32,

  new :: ((fn() -> Self)
    Self(0, 0)
  ),

  id :: ((fn(self: Self) -> Self)
    self
  )
  
;

main :: (fn() -> int) {
  p1 := Point(3, 4);
  p2 := Point.new();
  p3 := p2.id();
  0
};

export main;