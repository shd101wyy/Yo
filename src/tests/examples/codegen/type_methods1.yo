Point :: struct
  x : i32,
  y : i32,

  new :: ((() -> Self)
    Self(0, 0)
  ),

  id :: (((self: Self) -> Self)
    self
  )
  
;

main :: (() -> c_int) {
  p1 := Point(3, 4);
  p2 := Point.new();
  p3 := p2.id();
  0
};

export main;