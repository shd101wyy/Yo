extern
  add :
    fn(x : i32, y : i32) -> i32
;

compt_add :: (fn(x : i32, compt(y) : i32) -> i32)
  add(x, y)
;

main :: (fn() -> unit) {
  x := compt_add(1, 1);
  y := compt_add(2, 1);
  z := compt_add(3, 2);
};

export main;
