MyModule :: module {
  Self :: i32;

  zero :: (fn() -> i32)
    0
  ;

  id :: (fn(x : i32) -> i32)
    x
  ;

  export Self, zero, id;
};

main :: (fn() -> int) {
  z := MyModule.zero();
  zi := z.id();
  0
};

export main;