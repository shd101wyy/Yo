MyModule :: module {
  Self :: i32;

  zero :: (() -> i32)
    0
  ;

  id :: ((x : i32) -> i32)
    x
  ;

  export Self, zero, id;
};

main :: (() -> int) {
  z := MyModule.zero();
  zi := z.id();
  0
};

export main;