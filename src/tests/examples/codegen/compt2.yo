extern
  add :
    (x : i32, y : i32) -> i32
;

compt_i32 :: (() -> @(i32))
  12
;

Point :: struct
  x : i32,
  y : i32
;

use_point :: ((p : Point) -> i32)
  add(p.x, p.y)
;

main :: (() -> i32) {
  p :: Point(compt_i32(), 16);
  use_point(p)
};

export
  main
;