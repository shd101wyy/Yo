extern "C",
  add :
    fn(x : i32, y : i32) -> i32
;

main :: (fn() -> i32) {
  x := 1;
  y := 2;

  closure := {
    x,
    y,

    call :: ((fn(self: Self, z : i32) -> i32)
      add(self.x, add(self.y, z))
    )
  };

  result := closure.call(3);
  result
};

export main;