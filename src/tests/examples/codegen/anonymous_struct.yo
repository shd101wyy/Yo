extern "C",
  add :
    (x : i32, y : i32) -> i32
;

main :: (() -> i32) {
  x := 1;
  y := 2;

  closure := {
    x,
    y,

    call :: (((self: Self, z : i32) -> i32)
      add(self.x, add(self.y, z))
    )
  };

  result := closure.call(3);
  result
};

export main;