extern "C",
  eq :
    ((x : i32, y : i32) -> boolean),
  mul :
    ((x : i32, y : i32) -> i32),
  sub :
    ((x : i32, y : i32) -> i32)
;

factorial :: ((x : i32) -> i32)
  cond
    eq(x, 1) => 1,
    true => mul(x, recur(sub(x, 1)))
;

main :: (()-> i32)
  factorial(6)
;

export main;