extern "C",
  eq :
    (fn(x : i32, y : i32) -> bool),
  mul :
    (fn(x : i32, y : i32) -> i32),
  sub :
    (fn(x : i32, y : i32) -> i32)
;

factorial :: (fn(x : i32) -> i32)
  cond
    eq(x, 1) => 1,
    true => mul(x, recur(sub(x, 1)))
;

main :: (fn() -> i32)
  factorial(6)
;

export main;