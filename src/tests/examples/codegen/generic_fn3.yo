id :: ((@(T): Type, val: T) -> T) {
  id2 :: ((@(X): Type, val: X) -> X) 
    val
  ;
  id2(T, val)
};

main :: (() -> unit) {
  x := id(i32, 12);
  y := id(f64, 3.14);
  z := id(boolean, true);
};

export main;
