main :: (()-> unit) {
  Shape :: enum
    Circle(r: i32),
    Rectangle(width: i32, height: i32)
  ;

  c :: Shape.Circle(12);
  c1 := Shape.Circle(12);

  runt_value := 13;

  // c :: Shape.Circle(runt_value); // Expected error
  c2 := Shape.Circle(runt_value); // Expected error

  Color :: enum
    Red, Green, Blue
  ;
  c3 :: Color.Red;
  c4 := Color.Green;
}