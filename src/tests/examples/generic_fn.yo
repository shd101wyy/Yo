Point ::
  ((compt(T): Type) -> compt(Type))
  struct(x: T, y: T)
;

MakePoint ::
  ((compt(T): Type, value: T) -> Point(T))
  Point(T)(x: value, y: value)
;

main :: (()-> unit) {
  p := MakePoint(i32, 1);
  p := MakePoint(boolean, true);
}