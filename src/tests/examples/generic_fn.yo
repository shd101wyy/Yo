Point ::
  (fn(compt(T): Type) -> compt(Type))
  struct(x: T, y: T)
;

MakePoint ::
  (fn(compt(T): Type, value: T) -> Point(T))
  Point(T)(x: value, y: value)
;

main :: (fn() -> unit) {
  p := MakePoint(i32, 1);
  p2 := MakePoint(boolean, true);
}