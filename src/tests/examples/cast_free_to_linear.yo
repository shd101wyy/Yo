(compt(Data) : Linear) =
  struct(
    // Define the drop method for Data
    drop :: (
      (fn(self: Self) -> unit)
        ()
    )
  )
;


(compt(LinearI32) : Linear) = i32;
// Define the drop method for LinearI32
LinearI32Methods :: module {
  Self :: LinearI32;
  drop :: (fn(self: Self) -> unit) ();
  export Self, drop;
};

test :: (fn() -> unit) {
  x := 12;
  (y : LinearI32) = x;
  t :: typeof(y);
  drop(y);

  d := Data();
  drop(d);
};