main :: (()-> unit) {
  // arr := [1, 2, 3];
  
  // Create an array of integers with 3 elements
  {
    arr : Array(i32, 3);
    arr = [1, 2, 3];
  };
  
  {
    arr : Array(i32, 3);
    arr = array(1, 2, 3);
  };

  // Or create an array directly
  {
    arr := [1, 2, 3];
    x := arr(0);
  };

  // Get the array length
  {
    arr := [1, 2, 3];
    length :: arr.length;

    // define our own function:
    array_length :: ((forall(compt(T): Type, compt(n): usize), arr: [T; n]) -> (usize))
      n
    ;
    length2 := array_length(arr);
  }
};
