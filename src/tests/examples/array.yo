main :: (()-> unit) {
  // arr := [1, 2, 3];
  
  // Create an array of integers with 3 elements
  {
    arr : Array(i32, 3);
    arr = [1, 2, 3];
  };
  
  {
    arr : Array(i32, 3);
    arr = array(1, 2, 3);
  };

  // Or create an array directly
  {
    arr := [1, 2, 3];
    x := arr(0);
  };
};
