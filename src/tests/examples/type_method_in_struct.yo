extern
  add:
    fn(x: i32, y: i32)-> i32
;

Point :: struct
  x : i32,
  y : i32,

  // Let's only support to define type methods using :: for now
  new :: ((fn() -> Self)
    Self(0, 0)
  ),
  
  create :: ((fn(x: i32, y: i32) -> Self)
    Self(x, y)
  ),

  (+) :: ((fn(a: Self, b: Self)-> Self)
    Self((a.x `add` b.x), (a.y `add` b.y))
  ),

  add :: ((fn(a: Self, b: Self)-> Self)
    Self((a.x `add` b.x), (a.y `add` b.y))
  )
;

main :: (fn() -> unit) {
  a := Point.new();
  b := Point.create(3, 4);

  {
    x := (a.x `add` b.x);
    y := (a.y `add` b.y);
    c := Point (a.x `add` b.x), b.y;
  };

  {
    c := Point.add(a, b);
    d := a.add(b);
    e := (a + b);
    f := a.(Point.add)(b);
  };

  ()
}