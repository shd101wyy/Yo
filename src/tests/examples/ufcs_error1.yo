open import "std";

test :: (fn(compt(K) : Type, using(KeyEqual) : (K <: Eq(K))) -> unit) {
  (x : u8) = 1;
  (y : u8) = 2;
  compt_expect_error(
    flag := (x == y)
  );
  flag := (U8.Eq.==)(x, y);
};