open import "std";

// Mutal recursion
compt(is_even) : ((n : i32) -> boolean);
compt(is_odd) : ((n : i32) -> boolean);

is_even = _
  cond
    (n == 0) => true,
    true     => is_odd(n - 1)
;

is_odd  = _
  cond
    (n == 0) => false,
    true     => is_even(n - 1)
;