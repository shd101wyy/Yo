main :: (fn() -> unit) {
  (x: Tuple(a: i32, b: boolean)) = (12, true);
  a := x.a;
  b := x.1;

  Named :: struct(x: i32, y: boolean);
  p := Named(1, false);
  x2 := p.x;

  compt_expect_error(
    y := p.1
  );
};