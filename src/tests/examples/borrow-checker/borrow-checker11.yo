extern "Yo",
  Data   : Linear,
  malloc :
    fn()-> Data
;


test :: (fn() -> unit) {
  mut(x) := malloc();
  drop(x);
  compt_expect_error({
  y := (
    x = malloc());
    drop(x);
  }, "x is already consumed, so cannot mutate again");
};