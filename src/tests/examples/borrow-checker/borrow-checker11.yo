extern "Yo",
  Data   : Linear,
  malloc :
    ()-> Data
;


test :: (()-> unit) {
  mut(x) := malloc();
  consume(x);
  compt_expect_error({
  y := (
    x = malloc());
    consume(x);
  }, "x is already consumed, so cannot mutate again");
};