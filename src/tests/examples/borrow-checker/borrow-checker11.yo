extern "Yo",
  Data   : Linear,
  malloc :
    ()-> Data
;


test :: (()-> unit) {
  mut(x) := malloc();
  drop(x);
  compt_expect_error({
  y := (
    x = malloc());
    drop(x);
  }, "x is already consumed, so cannot mutate again");
};