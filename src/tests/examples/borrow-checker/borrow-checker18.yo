extern "Yo",
  Data   : Type,
  malloc :
    fn()-> Data
;


test :: (fn(linear_array: Array(Data, 2), free_array: Array(i32, 2))-> unit) {
  x := free_array(0); // Access Free value is allowed.  
  {
    // Access Linear value is not allowed, 
    // compt_expect_error(
    //   z := linear_array(0),
    //   "error: cannot access linear value in an array"  
    // );

    // but reference is allowed.
    borrow &(linear_array(0)), first_element_ref => {
      ();
    };
  };
  drop(linear_array);
};