extern "C",
  malloc  :
    ()-> ^(i32)
;

test :: (()-> unit) {
  mut(x) := malloc();
  borrow &(x), x_ref -> {
    y := x_ref.*.*;
    compt_expect_error(
      consume(x),
      "error: x is borrowed"
    );
  };
  consume(x); // Allowed
}