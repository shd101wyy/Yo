extern "C",
  malloc  :
    ()-> ^(i32)
;

test :: ((flag: boolean)-> unit) {
  x := malloc();

  cond 
    flag -> {
      borrow &(x), (ref1)-> {
        ();
      };
    },
    true -> {
      borrow &(x), (ref2)-> {
        ();
      };
    }
  ;

  consume(x);
};