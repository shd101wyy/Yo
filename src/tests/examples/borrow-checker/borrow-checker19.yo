extern "Yo",
  Data   : Linear,
  malloc :
    ()-> Data
;


Person :: struct
  age: i32,
  name: Data
;

test :: (()-> unit) {
  name := malloc();
  p := Person(age: 12, name: name);

  compt_expect_error(
    borrow &(name), name_ref -> {
      ();
    },
    "error: 'name' is already consumed in the struct 'p'"
  );

  drop(p);
};