extern "Yo",
  Data   : Linear,
  malloc :
    ()-> Data
;


Person :: struct
  age: i32,
  name: Data,
  job: Data
;

test :: ((p: Person)-> unit) {
  age := p.age;
  { name, job } := p;

  drop(name);
  drop(job);
};