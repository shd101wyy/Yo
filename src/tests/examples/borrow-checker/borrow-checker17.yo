extern "Yo",
  Data   : Type,
  malloc :
    fn()-> Data
;


Person :: struct
  age: i32,
  name: Data,
  job: Data
;

test :: (fn(p: Person)-> unit) {
  age := p.age;
  { name, job } := p;

  drop(name);
  drop(job);
};