extern "C",
  malloc  :
    ()-> ^(i32)
;

Person :: struct
  age: i32,
  name: ^(i32),
  job: ^(i32)
;

test :: ((p: Person)-> unit) {
  age := p.age;
  { name, job } := p;

  consume(name);
  consume(job);
};