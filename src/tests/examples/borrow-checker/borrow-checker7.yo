extern "Yo",
  Data: Linear,
  malloc  :
    ()-> Data
;


test :: (()-> unit) {
  x := malloc();
  drop(x);
  compt_expect_error(
    borrow &(x), x_ref -> {
      ();
    },
    "error: `x` is already consumed, so it cannot be borrowed as a reference"
  );
}