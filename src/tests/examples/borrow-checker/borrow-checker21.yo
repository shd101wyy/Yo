extern "C",
  malloc  :
    ()-> ^(i32)
;

Person :: struct
  age: i32,
  name: ^(i32),
  job: ^(i32)
;

test0 :: ((p: Person)-> Person)
  p
;

test :: ((p: Person)-> Person) {
  mut(p) := p;

  age := p.age;

  {
    borrow &!(p.name), name_ref-> {
      old_name := (name_ref.* = malloc());
      consume(old_name);
    };
  };

  {
    oldName := (p.name = malloc());
    consume(oldName);
  };

  p
};