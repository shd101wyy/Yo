open import "std";
open import "std/libc/stdio.yo";

// Pointer arithmetic example
main :: (fn() -> unit) {
  ch := chan(i32); // unbuffered channel

  async { ch <- 12; }; // send
  value := <-(ch); // receive
  printf("%d\n", value);
};

export main;