test :: (() -> compt(unit)) {
  // This is a compt function.
  compt_expect_error(
    a : i32 // runtime variable not allowed
  );
  compt_expect_error(
    b := 12 // runtime variable not allowed
  );

  (compt(c) : i32) = 1;
  d :: 13;
};