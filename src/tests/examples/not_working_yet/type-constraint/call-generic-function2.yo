import { genericId1 } from "./function-type-constraint.yo";
import { Id } from "../classes/id.yo";

implements Id<i32> {
  id: (x: i32)-> i32 {
    x
  }
}

let main = ()-> {
  let x = genericId1<i32>(12);
}