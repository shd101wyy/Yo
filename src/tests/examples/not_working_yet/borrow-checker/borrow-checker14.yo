extern "Yo",
  Data   : Type,
  malloc :
    fn()-> Data
;


test :: (fn(flag: bool)-> unit) {
  x := malloc();

  cond 
    flag => {
      borrow &(x), (ref1)=> {
        ();
      };
    },
    true => {
      borrow &(x), (ref2)=> {
        ();
      };
    }
  ;

  drop(x);
};