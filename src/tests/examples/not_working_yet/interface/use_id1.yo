// Let's try importing { id } first.  
import { id } from "./id.yo"; 
// then import the type Coord.
import { Coord } from "./coord.yo";  

let main = ()-> {
  let c1 = Coord {
    x: 1,
    y: 2
  };
  let c2 = id<Coord>(c1);
}