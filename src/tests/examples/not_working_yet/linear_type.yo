extern
  malloc:
    ()-> ^(i32),
  consume:
    (x: ^(i32))-> unit
;

def test:
  ()-> unit, {
  malloc(); // This should give error, as its temp variable is not consumed.
  /*
  a := malloc();
  consume(a);
  // 'a' is consumed and cannot be used after this point
  // consume(a); // gives an error
  // b := a    ; // gives an error
  */
};