open import "std/libc/stdio";
open import "std/string";
{ Path } :: import "std/path";

main :: (fn() -> unit) {
  p := Path.new(`/usr/local/bin`);
  name := p.to_string();
  printf("path: %s\n", name.to_cstr().ptr().unwrap());
};

export main;
