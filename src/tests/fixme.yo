main :: (fn() -> unit) {
  {
    expect_val :: (fn(x : i32) -> unit) {
      assert(i32(x) == i32(5));
    };
    expect_val(5);

    // extra line for testing C codegen
    x := 666;
  };
};

export main;