main :: (fn() -> unit) {  
  op := Option(i32).Some(12);

  match(op,
    .Some(_) => {
      assert(true);
    },
    .None => {
      assert(false, "Expected Some, got None");
    }
  );
  
  return ();
};

export main;
