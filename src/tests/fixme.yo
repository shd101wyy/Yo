pragma(Pragma.AllowUnsafe);
open(import("std/libc/stdio"));
main :: (fn() -> unit)({
  (i : i32) = i32(0);
  while(i < i32(3), {
    unsafe(_r := putchar(int(i32(65) + i)));
    i = (i + i32(1));
  });
  unsafe(_n := putchar(int(10)));
});
export(main);
