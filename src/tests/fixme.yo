main :: (fn(using(io : IO)) -> unit) {
  my_async :: io.async;
  my_await :: io.await;

  fut := my_async(() => {
    return 12;
  });
  result := my_await(fut);
  assert(result == 12);
};

export main;