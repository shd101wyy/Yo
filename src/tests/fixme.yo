main :: (fn(using(io : IO)) -> unit) {
  my_async :: io.async;
  my_await :: io.await;

  (action : Impl(Fn() -> i32)) =
    () => {
      return 12;
    };

  fut := my_async(action);
  result := my_await(fut);
  assert(result == 12);
};

export main;