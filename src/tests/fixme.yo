open import "std/string";
open import "std/fmt";

main :: (fn() -> unit) {
  x := String.from("Hello,");
  y := String.from(" world!");
  full := (x + y);
  (z : [u8]) = "Hello, world!";
  z_str := z.to_string();
  assert(full == z_str);
};

export main;