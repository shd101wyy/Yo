{
  // Basic i8 values
  a :: i8(50);
  b :: i8(25);
  compt_expect_error(prod :: (a * b));       // 1250i8 (may overflow)
};

main :: (fn() -> unit) {
  // Basic i8 values
  c :: i8(50);
  d :: i8(25);
  compt_expect_error(prod :: (c * d));       // 1250i8 (may overflow)
};

export main;
