String :: struct(
  index_of :: (fn(self : Self, substring : Self, (from_index : usize) ?= 0) -> Option(usize))(
    .None
  ),

  includes :: (fn(self : Self, substring : Self) -> bool)(
    match(self.index_of(substring),
      .Some(_) => true,
      .None => false
    ))
);