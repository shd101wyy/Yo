open import "std/libc/stdio";

MyBox :: object(
  value: i32
);

impl(MyBox, Dispose(
  dispose : (fn(self: Self) -> unit)({
    printf("Disposing MyBox with value: %d\n", self.value);
  })
));

main :: (fn() -> unit) {
  MyBox(42);
};
export main;