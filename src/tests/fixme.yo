main :: (fn() -> unit) {
  apply :: (fn(
    forall(T : Type),
    x : T,
    using(op : (fn(a : T) -> T))
  ) -> T)(
    op(x)
  );

  given(inc) := (fn(a : i32) -> i32)(a + 1);
  result := apply(41);
  assert(result == 42, "forall + using failed");
};
export main;