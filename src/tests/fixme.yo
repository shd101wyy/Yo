// open import "std/fmt";
open import "std/string";
open import "std/collections/array_list";

/*
test_slice :: (fn() -> unit) {
  println("--- Testing slice ---");
  
  s := `Hello, World!`;
  
  slice1 := s.slice(usize(0), usize(5));
  print("slice(0, 5): ");
  println(slice1);
  
  slice2 := s.slice(usize(7), usize(12));
  print("slice(7, 12): ");
  println(slice2);
  
  slice3 := s.slice(usize(0), usize(13));
  print("slice(0, 13): ");
  println(slice3);
  
  unicode_str := `你好世界`;
  slice4 := unicode_str.slice(usize(0), usize(2));
  print("Unicode slice(0, 2): ");
  println(slice4);
  
  slice5 := unicode_str.slice(usize(2), usize(4));
  print("Unicode slice(2, 4): ");
  println(slice5);
};

test_index_of :: (fn() -> unit) {
  println("--- Testing index_of ---");
  
  s := `Hello, World!`;
  
  idx1 := s.index_of(`World`);
  match(idx1,
    .Some(i) => {
      print("index_of('World'): ");
      println(i);
    },
    .None => println("index_of('World'): not found")
  );
  
  idx2 := s.index_of(`Hello`);
  match(idx2,
    .Some(i) => {
      print("index_of('Hello'): ");
      println(i);
    },
    .None => println("index_of('Hello'): not found")
  );
  
  idx3 := s.index_of(`xyz`);
  match(idx3,
    .Some(i) => {
      print("index_of('xyz'): ");
      println(i);
    },
    .None => println("index_of('xyz'): not found")
  );
  
  unicode_str := `你好世界`;
  idx4 := unicode_str.index_of(`世界`);
  match(idx4,
    .Some(i) => {
      print("Unicode index_of('世界'): ");
      println(i);
    },
    .None => println("Unicode index_of('世界'): not found")
  );
};

test_includes :: (fn() -> unit) {
  println("--- Testing includes ---");
  
  s := `Hello, World!`;
  
  has_world := s.includes(`World`);
  print("includes('World'): ");
  println(has_world);
  
  has_hello := s.includes(`Hello`);
  print("includes('Hello'): ");
  println(has_hello);
  
  has_xyz := s.includes(`xyz`);
  print("includes('xyz'): ");
  println(has_xyz);
  
  has_comma := s.includes(`,`);
  print("includes(','): ");
  println(has_comma);
  
  unicode_str := `你好世界`;
  has_unicode := unicode_str.includes(`世`);
  print("Unicode includes('世'): ");
  println(has_unicode);
};

test_split :: (fn() -> unit) {
  println("--- Testing split ---");
  
  s := `Hello,World,Test`;
  parts := s.split(`,`);
  print("split(','): ");
  println(parts.len());
  println(" parts:");
  
  i := usize(0);
  while ((i < parts.len())),
        (i = (i + usize(1))),
  {
    part_opt := parts.get(i);
    match(part_opt,
      .Some(part) => {
        print("  [");
        print(i);
        print("]: ");
        println(part);
      },
      .None => ()
    );
  };
  
  str2 := `one two three`;
  parts2 := str2.split(` `);
  print("split(' '): ");
  println(parts2.len());
  println(" parts:");
  
  j := usize(0);
  while ((j < parts2.len())),
        (j = (j + usize(1))),
  {
    part_opt := parts2.get(j);
    match(part_opt,
      .Some(part) => {
        print("  [");
        print(j);
        print("]: ");
        println(part);
      },
      .None => ()
    );
  };
  
  str3 := `abc`;
  parts3 := str3.split(``);
  print("split(''): ");
  println(parts3.len());
  println(" chars:");
  
  k := usize(0);
  while ((k < parts3.len())),
        (k = (k + usize(1))),
  {
    part_opt := parts3.get(k);
    match(part_opt,
      .Some(part) => {
        print("  [");
        print(k);
        print("]: ");
        println(part);
      },
      .None => ()
    );
  };
};
*/

main :: (fn() -> unit) {
  arr := ArrayList(String).new();
  arr.push(`Hello`);
  arr.push(`World`);
  // println(arr.get(usize(0)).unwrap());

  // println("=== Testing String Methods ===");
  // println("");
  // 
  // test_slice();
  // println("");
  // 
  // test_index_of();
  // println("");
  // 
  // test_includes();
  // println("");
  // 
  // test_split();
  // println("");
  // 
  // println("All tests completed!");
  ();
};

export main;