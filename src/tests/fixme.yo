// @skip_prelude
swap :: (fn(x : *(i32), y : *(i32)) -> unit) {
  temp := x.*;
  x.* = y.*;
  y.* = temp;
};

a :: 1;
b :: 2;
swap(&(a), &(b));

compt_print(a); // Expected to print 2.
compt_print(b); // Expected to print 1.
compt_print(&(a));