open(import("std/string"));
open(import("std/fmt"));
main :: (fn(io : Io) -> unit)({
  {
    s := String.from("scoped_value_here");
    println(s);
  };
  println(String.from("AFTER_BLOCK"));
});
export(main);
