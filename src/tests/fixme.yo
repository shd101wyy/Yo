{ yield } :: import "std/async";

main :: (fn(using(io : IO)) -> unit)({
  task := io.async((using(io : IO)) => {
    a := i32(1);
    b := i32(2);
    io.await(yield());
    c := i32(3);
    io.await(yield());
    result := (a + c);
    return result;
  });
  r := io.await(task);
});
export main;
