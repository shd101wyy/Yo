open import "std/collections/array_list.yo";

main :: (fn() -> unit) {
  list := ArrayList(i32).new();
  list.push(i32(10));
  list.push(i32(20));
  list.push(i32(30));
  
  // assert(list.len() == usize(3));
  assert(list.get(usize(0)).unwrap() == i32(10));
  // assert(list.get(usize(1)).unwrap() == i32(20));
  // assert(list.get(usize(2)).unwrap() == i32(30));
};
export main;