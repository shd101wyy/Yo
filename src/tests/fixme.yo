// Test simple recur
factorial :: (fn(x : i32) -> i32) {
  compt_print("x = ", x);
  return cond(
    (x <= 0) => 1,
    true => (x * recur((x - 1)))
  );
};

result :: factorial(i32(3));
compt_print("result = ", result);
