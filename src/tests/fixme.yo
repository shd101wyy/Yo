open import "std/string";
open import "std/path";
open import "std/collections/hash_set";

main :: (fn() -> unit) {
  path1 := Path.new(`src/main.yo`);

  set := HashSet(Box(i32)).new();
  set.add(Box(i32)(10));
  assert(set.has(Box(i32)(10)));
};

export main;
