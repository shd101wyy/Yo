open(import("std/string"));
open(import("std/fmt"));

main :: (fn(io : Io) -> unit)({
  sep := `\0`;
  key := `a${sep}b`;
  println(key.len().to_string());
  println(`${key.as_bytes()(usize(1)) == u8(0)}`);
});
export(main);
