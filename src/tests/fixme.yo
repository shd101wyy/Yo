// open import "std/fmt/to_string";
// It should automatically `import "std/fmt/to_string"` when using the backtik template strings.

main :: (fn() -> unit) {
  x := `hello, ${123} and ${true} and ${3 + 4}.`;
};

export main;
