import("std/fmt/to_string");

// Auto-generated main function for test: Test the `where` scoping in functions
main :: (fn() -> unit) {
  Container :: (fn(compt(T): Type) -> compt(Type))(
    object(
      value : T,

      // This function has where(T <: Eq(T))
      equals :: (fn(
        self: Self, 
        other: T,
        where(T <: Eq(T))
      ) -> bool)(
        self.value == other
      )
    )
  );
};

export main;
