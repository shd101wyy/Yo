ret_i32 :: (fn(a : i32) -> i32) {
  return a;
};

x :: ret_i32(5); // This should work - CTFE enabled!
compt_print(x);

add :: (fn(a : i32, b : i32) -> i32) {
  return (a + b);
};

y :: add(3, 4); // This should work - CTFE enabled!
compt_print(y);