main :: (fn() -> unit) {
  number := 3;
  four :: 4;
  match(number,
    (1 | 2) => {
      panic("Shouldn't match 1 | 2");
    },
    ( 3
    | four
    | 5) => {
      assert(true, "Should match 3 | 4 | 5");
    },
    _ => {
      panic("Shouldn't match this");
    }
  );
};

export main;