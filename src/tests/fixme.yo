// open import "std/fmt";
open import "std/string";
open import "std/collections/array_list";
open import "std/fmt";

main :: (fn() -> unit) {
  s := `你好`;
  r := s.at(0).unwrap();
  s2 := r.to_string();
  println(s2);


  ();

  // code := u32(15);
  // a := ((code >> 6) & 0x1F);
  // b := u32(0xC0);
  // x := u8(b | a)
};

export main;