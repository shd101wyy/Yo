open import "std/libc/stdio";

test_fn :: (fn(comptime(T) : Type, v : T) -> T) {
  return v;
};

main :: (fn(using(io : IO)) -> unit)({
  x := test_fn(i32, i32(42));
  assert(x == 42);
});
export main;
