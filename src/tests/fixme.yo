{ assert } :: import("std/assert");
MyVal :: ref(enum(UnitVal, IntVal(v : i32)));
// Body is the unit literal, so the whole call is elided at compile time.
eat_it :: (fn(x : MyVal) -> unit)(());
main :: (fn() -> unit)({
  eat_it(MyVal.UnitVal);
  assert(true, "ignored");
});
export(main);
