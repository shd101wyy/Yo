//! evaluator/types/validation — dispose-function signature validation.
//!
//! Status: stub. The intended export is a
//! `validate_dispose_function(module_element, token)` that checks a
//! `dispose : fn(self : Self) -> unit` field against the expected
//! signature. Nothing in the evaluator currently calls it
//! (the dispose-signature check is enforced elsewhere), and no code
//! references this module yet.
pragma(Pragma.AllowUnsafe);
