
type Data: Linear;
extern "C" {
  malloc: ()-> Data;
  consume: (x: Data)-> unit;
  length: (x: &Data)-> i32;
}

let test = (x: Data @1, y: Data @3, z: Data @2)-> {
  let m = malloc();

  consume(m);
  consume(y);
  consume(z);
  consume(x);
}