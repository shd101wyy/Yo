# Roadmap

1. [ ] Combine `effect` with `class`.
   1. [ ] Migrate `class` to `interface`.
   2. [ ] Migrate `instance` to `implement`.
   3. [ ] Get the module import/export for `interface` working.
   4. [ ] Migrate `effect` to `interface`.
2. [ ] Get the type constraint working
   1. [ ] Get the type constraint working for `interface`.
   2. [ ] Get the type constraint working for `implement`.
   3. [ ] Get the type constraint working for `function`.
3. Different types of `closure`
   1. `()=> ()`
   2. `[write]()=> {}`
   3. `[read]()=> {}`
