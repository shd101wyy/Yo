diff --git a/yo-self/codegen/functions/declarations.yo b/yo-self/codegen/functions/declarations.yo
index 29fd3b616..e2337694e 100644
--- a/yo-self/codegen/functions/declarations.yo
+++ b/yo-self/codegen/functions/declarations.yo
@@ -696,11 +696,14 @@ generate_function_declarations :: (fn(context : CodeGenContext) -> unit)({
   // depth 2 (the fallback registration above cascades one hop per round).
   (aro_round : usize) = usize(0);
   while(aro_round < usize(2), {
-    aro_keys := context.functions.keys();
-    (aro_go : bool) = true;
-    while(aro_go, {
+    // INSERTION order (function_order), not HashMap bucket order: the callee's
+    // return-type resolution must be deterministic across s1/s2 and reach the
+    // delegate before its caller. Index + re-read len() so specs registered
+    // mid-loop are covered.
+    (aro_fi : usize) = usize(0);
+    while(aro_fi < context.function_order.len(), {
       match(
-        aro_keys.next(),
+        context.function_order.get(aro_fi),
         .Some(aro_fid) => match(
           context.get_function_entry(aro_fid.clone()),
           .Some(aro_entry) => {
@@ -713,20 +716,22 @@ generate_function_declarations :: (fn(context : CodeGenContext) -> unit)({
           },
           .None => ()
         ),
-        .None => {
-          aro_go = false;
-        }
+        .None => ()
       );
+      aro_fi = (aro_fi + usize(1));
     });
     aro_round = (aro_round + usize(1));
   });
   // Iterate functions by id so the exported-linkage predicate (keyed by func_id)
-  // matches generate_function exactly.
-  keys := context.functions.keys();
-  (go : bool) = true;
-  while(go, {
+  // matches generate_function exactly. INSERTION order (function_order), NOT
+  // HashMap bucket order, so the prototype loop agrees with the body loop
+  // (generation.yo) AND is deterministic across s1/s2. Index + re-read len()
+  // so specs registered mid-loop are declared. See
+  // issues/yo-self-emission-order-nondeterminism.md.
+  (fi : usize) = usize(0);
+  while(fi < context.function_order.len(), {
     match(
-      keys.next(),
+      context.function_order.get(fi),
       .Some(func_id) => match(
         context.get_function_entry(func_id.clone()),
         .Some(entry) => {
@@ -744,10 +749,9 @@ generate_function_declarations :: (fn(context : CodeGenContext) -> unit)({
         },
         .None => ()
       ),
-      .None => {
-        go = false;
-      }
+      .None => ()
     );
+    fi = (fi + usize(1));
   });
   em.emit_declaration_line("/// Closure vtable instances");
   generate_closure_vtable_declarations(context);
diff --git a/yo-self/codegen/functions/generation.yo b/yo-self/codegen/functions/generation.yo
index 9d75072b0..7df6ad32b 100644
--- a/yo-self/codegen/functions/generation.yo
+++ b/yo-self/codegen/functions/generation.yo
@@ -551,11 +551,16 @@ generate_all_functions :: (fn(context : FunctionGenerationContext) -> unit)({
   generate_ref_struct_constructor_functions(context);
   generate_ref_enum_constructor_functions(context);
   generate_closure_constructor_functions(context);
-  it := context.base.functions.keys();
-  (go : bool) = true;
-  while(go, {
+  // Iterate in INSERTION order (function_order), NOT HashMap bucket order, so
+  // body emission — and the specializations created during it — are ordered
+  // deterministically across the TS-compiled s1 and self-compiled s2. Index by
+  // position and re-read len() each pass so specs registered mid-emission are
+  // still emitted. Mirrors TS `for (funcId in context.functions)` (insertion
+  // order). See issues/yo-self-emission-order-nondeterminism.md.
+  (fi : usize) = usize(0);
+  while(fi < context.base.function_order.len(), {
     match(
-      it.next(),
+      context.base.function_order.get(fi),
       .Some(fid) => match(
         context.base.get_function_entry(fid),
         .Some(entry) => {
@@ -571,10 +576,9 @@ generate_all_functions :: (fn(context : FunctionGenerationContext) -> unit)({
         },
         .None => ()
       ),
-      .None => {
-        go = false;
-      }
+      .None => ()
     );
+    fi = (fi + usize(1));
   });
 });
 /// The C argument list for the `__yo_user_main` call: each runtime parameter
diff --git a/yo-self/codegen/utils/index.yo b/yo-self/codegen/utils/index.yo
index 866569746..efbf0e9ea 100644
--- a/yo-self/codegen/utils/index.yo
+++ b/yo-self/codegen/utils/index.yo
@@ -113,6 +113,14 @@ CodeGenContext :: ref(
     types : HashMap(String, CodegenTypeEntry),
     /// Collected functions that need to be generated (keyed by func_id).
     functions : HashMap(String, CodegenFunctionEntry),
+    /// Insertion order of func_ids registered into `functions`. Codegen emits
+    /// functions by iterating THIS list, mirroring TS's
+    /// `for (funcId in context.functions)` (JS objects iterate in insertion
+    /// order). yo-self's HashMap iterates in bucket order, which is unstable
+    /// across the TS-compiled s1 and the self-compiled s2 when the function
+    /// set changes — breaking the stage2≡stage3 fixpoint. See
+    /// issues/yo-self-emission-order-nondeterminism.md.
+    function_order : ArrayList(String),
     /// Extern functions (keyed by TypeId string).
     extern_functions : HashMap(String, CodegenExternFnEntry),
     /// Array struct types that need to be generated (keyed by C name).
@@ -228,6 +236,7 @@ impl(
     Self(
       types : HashMap(String, CodegenTypeEntry).new(),
       functions : HashMap(String, CodegenFunctionEntry).new(),
+      function_order : ArrayList(String).new(),
       extern_functions : HashMap(String, CodegenExternFnEntry).new(),
       array_struct_types : HashMap(String, ArrayStructInfo).new(),
       iso_types : HashMap(String, IsoTypeInfo).new(),
@@ -320,6 +329,12 @@ impl(
   ),
   /// Register a reachable function to be generated (idempotent on func_id).
   register_function : (fn(self : Self, func_id : String, value : EvalValue, c_name : String) -> unit)({
+    // Record first-registration order (insertion order) so codegen emits
+    // functions deterministically, mirroring TS `for..in` semantics rather
+    // than HashMap bucket order. Re-registration keeps the original position.
+    if(!(self.functions.contains_key(func_id.clone())), {
+      _o := self.function_order.push(func_id.clone());
+    });
     _r := self.functions.set(func_id, CodegenFunctionEntry(value : value, c_name : c_name));
     ()
   }),
