diff --git a/std/async/channel.yo b/std/async/channel.yo
index 1319cffd5..e8d9d18fb 100644
--- a/std/async/channel.yo
+++ b/std/async/channel.yo
@@ -9,10 +9,16 @@
 //! design: Yo's async runtime is per-thread). Across OS threads use
 //! `std/sync/channel`.
 //!
-//! A blocked `send`/`recv` re-checks on a 1ms timer tick (the millisecond
-//! granularity of the timer contract), so cross-task handoff latency is up
-//! to 1ms when the channel has to wait; the fast path (space/data already
-//! available) never suspends.
+//! A blocked `send`/`recv` is WAKER-BASED: the task publishes a waker on the
+//! side it is waiting for and suspends, and the other side wakes it when it
+//! makes progress — a `recv` that takes a value wakes one blocked sender, a
+//! `send` that buffers one wakes one blocked receiver, and `close` wakes
+//! everybody so they can observe the closure. So a hand-off costs a loop turn
+//! rather than a millisecond; the fast path (space or data already available)
+//! still never suspends at all. Until 2026-09-11 both sides re-checked on a
+//! 1 ms timer tick, which put a millisecond floor under every hand-off and
+//! capped a producer/consumer pair at ~1000 values per second
+//! (`plans/backlog/WAKER_BASED_SCHEDULING.md` stage 3).
 //!
 //! ```rust
 //! { Channel } :: import "std/async/channel";
@@ -55,8 +61,8 @@
 //! unstable — `Channel.recv` answers `Option(T)`, where `.None` means the
 //! channel closed, and whether that should be a `Result` the way `try_recv`'s
 //! answer now is remains open. (`Receiver.recv`, the split's consumer, IS
-//! already a `Result(T, TryRecvError)`.) The 1 ms timer tick above is an
-//! implementation choice a waker-based rewrite would remove, which changes
+//! already a `Result(T, TryRecvError)`.) The 1 ms timer tick is GONE as of
+//! 2026-09-11 — both sides are waker-based now — which changes
 //! handoff latency but no signature.
 //!
 //! `new`, `send`, `try_send`, `try_recv`, `close`, `is_closed` and `len` are
@@ -67,7 +73,7 @@
 // polls both does not need two vocabularies for one distinction.
 { TryRecvError } :: import("../sync/channel");
 { assert } :: import("../assert");
-IO_timer :: import("../sys/timer");
+{ Waker, Park } :: import("./waker.yo");
 { Stream } :: import("./stream");
 
 /// A bounded FIFO channel for same-thread async tasks.
@@ -90,7 +96,17 @@ Channel :: (fn(comptime(T) : Type) -> comptime(Type))(
       /// Set once the `Receiver(T)` handle has been dropped, which makes every
       /// further `send` fail instead of suspending on a channel nobody will
       /// drain.
-      _recv_dropped : bool
+      _recv_dropped : bool,
+      /// Tasks suspended in `send` waiting for space, in arrival order. A
+      /// `recv` or `try_recv` that frees a slot wakes the front one; `close`
+      /// and `_mark_receiver_dropped` wake them all, because after either of
+      /// those every one of them has an answer waiting.
+      _send_waiters : ArrayList(Waker),
+      /// Tasks suspended in `recv` waiting for data, same discipline. A `send`
+      /// or `try_send` that buffers a value wakes the front one; `close` wakes
+      /// them all, because a closed-and-drained channel resolves `.None`
+      /// rather than waiting.
+      _recv_waiters : ArrayList(Waker)
     )
   )
 );
@@ -105,7 +121,9 @@ impl(
       _capacity : capacity,
       _closed : false,
       _senders : usize(0),
-      _recv_dropped : false
+      _recv_dropped : false,
+      _send_waiters : ArrayList(Waker).new(),
+      _recv_waiters : ArrayList(Waker).new()
     )
   }),
   /// Send a value, suspending while the buffer is full. Resolves to
@@ -129,7 +147,13 @@ impl(
           true => ()
         );
         if(waiting, {
-          io.await(IO_timer.sleep(u64(1)), io);
+          // Publish the waker, THEN suspend — the order that cannot lose a
+          // wake. Re-check after waking rather than trusting it: the waker
+          // says "space may have appeared", and a `try_send` from a third
+          // task can have taken that slot in between.
+          p := Park.new();
+          self._send_waiters.push(p.waker());
+          io.await(p.wait(io), io);
         });
       });
       cond(
@@ -139,6 +163,7 @@ impl(
           // No suspension between the capacity check and this push, so no
           // other task can interleave (single-threaded cooperative loop).
           self._buf.push(value);
+          self._wake_one_receiver();
           return(Result(unit, T).Ok(()));
         }
       );
@@ -160,12 +185,15 @@ impl(
           true => ()
         );
         if(waiting, {
-          io.await(IO_timer.sleep(u64(1)), io);
+          p := Park.new();
+          self._recv_waiters.push(p.waker());
+          io.await(p.wait(io), io);
         });
       });
       cond(
         (self._buf.len() > usize(0)) => {
           v := self._buf.remove(usize(0));
+          self._wake_one_sender();
           return(Option(T).Some(v));
         },
         true => return(Option(T).None)
@@ -181,6 +209,7 @@ impl(
       (self._buf.len() >= self._capacity) => Result(unit, T).Err(value),
       true => {
         self._buf.push(value);
+        self._wake_one_receiver();
         Result(unit, T).Ok(())
       }
     )
@@ -201,6 +230,7 @@ impl(
     cond(
       (self._buf.len() > usize(0)) => {
         v := self._buf.remove(usize(0));
+        self._wake_one_sender();
         Result(T, TryRecvError).Ok(v)
       },
       self._closed => Result(T, TryRecvError).Err(TryRecvError.Disconnected),
@@ -211,12 +241,57 @@ impl(
   /// `recv` keeps draining buffered values, then resolves `.None`.
   close : (fn(self : Self) -> unit)({
     self._closed = true;
+    // Wake EVERY waiter on both sides, not one: after a close each of them has
+    // an answer waiting — a blocked `send` fails, a blocked `recv` drains and
+    // then resolves `.None` — and waking one would leave the rest suspended
+    // for good. A lost wake here is a HANG, not a wrong value, which is why
+    // `tests/async/channel.test.yo` asserts that all N woken waiters ran.
+    self._wake_all();
   }),
   /// Record that this channel's `Receiver` handle is gone: every further
   /// `send` fails, and a task suspended on a full buffer finds out on its
   /// next tick. Internal — `Receiver`'s `dispose` is the only caller.
   _mark_receiver_dropped : (fn(self : Self) -> unit)({
     self._recv_dropped = true;
+    // Every suspended sender now fails; none of them will be woken by a
+    // receive that is never going to happen.
+    self._wake_all_senders();
+  }),
+  /// Wake the longest-waiting receiver, if any. Called wherever a value
+  /// becomes available.
+  _wake_one_receiver : (fn(self : Self) -> unit)(
+    cond(
+      (self._recv_waiters.len() > usize(0)) => {
+        w := self._recv_waiters.remove(usize(0));
+        w.wake();
+      },
+      true => ()
+    )
+  ),
+  /// Wake the longest-waiting sender, if any. Called wherever a slot frees up.
+  _wake_one_sender : (fn(self : Self) -> unit)(
+    cond(
+      (self._send_waiters.len() > usize(0)) => {
+        w := self._send_waiters.remove(usize(0));
+        w.wake();
+      },
+      true => ()
+    )
+  ),
+  /// Wake every suspended sender and drop the queue.
+  _wake_all_senders : (fn(self : Self) -> unit)({
+    while(runtime(self._send_waiters.len() > usize(0)), {
+      w := self._send_waiters.remove(usize(0));
+      w.wake();
+    });
+  }),
+  /// Wake every suspended task on both sides.
+  _wake_all : (fn(self : Self) -> unit)({
+    self._wake_all_senders();
+    while(runtime(self._recv_waiters.len() > usize(0)), {
+      w := self._recv_waiters.remove(usize(0));
+      w.wake();
+    });
   }),
   /// True once `close()` has been called.
   is_closed : (fn(self : Self) -> bool)(
