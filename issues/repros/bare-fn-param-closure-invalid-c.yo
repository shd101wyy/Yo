open(import("std/string"));
open(import("std/fmt"));
takes_bare :: (fn(f : (fn(a : i32) -> i32), v : i32) -> i32)(
  f(v)
);
main :: (fn() -> unit)({
  k := i32(10);
  println(`bare fn param with a lambda: ${takes_bare(((y) => (y + k)), i32(5))}`);
  ();
});
export(main);
