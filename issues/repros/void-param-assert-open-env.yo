open(import("std/string"));
{ assert } :: import("std/assert");
open(import("std/env"));

main :: (fn() -> unit)({
  result := env.get(String.from("NOPE_VAR"));
  assert(result.is_none());
});
export(main);
