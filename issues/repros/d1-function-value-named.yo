{ Thread } :: import("std/thread");
{ ArrayList } :: import("std/collections/array_list");
g := ArrayList(i32).new();
_worker :: (fn(io : Io) -> unit)({
  g.push(i32(1));
});
main :: (fn() -> unit)({
  t := Thread(unit).spawn(_worker);
  g.push(i32(2));
  t.join();
});
export(main);
