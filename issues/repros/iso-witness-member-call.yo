{ assert } :: import("std/assert");
main :: (fn() -> unit)({
  x := box(123);
  z := ^(x);
  match(
    z,
    .Some(_) => assert(true),
    .None => assert(false, "unreachable")
  );
});
export(main);
