diff --git a/yo-self/codegen/exprs/other_fn_call.yo b/yo-self/codegen/exprs/other_fn_call.yo
index a799f2350..8774822ea 100644
--- a/yo-self/codegen/exprs/other_fn_call.yo
+++ b/yo-self/codegen/exprs/other_fn_call.yo
@@ -21,7 +21,7 @@ open(import("std/string"));
 { get_type_trait_methods_by_name, type_id_or_empty } :: import("../../evaluator/values/type_trait_methods.yo");
 { get_func_type, is_control_fn, is_effect_record_member } :: import("../../function_value.yo");
 { TypeValue, FuncMeta } :: import("../../types/definitions.yo");
-{ is_reference_struct_type, is_reference_enum_type, is_function_type, is_unit_type, is_array_type, is_dyn_type, is_struct_type, is_enum_type } :: import("../../types/guards.yo");
+{ is_reference_struct_type, is_reference_enum_type, is_function_type, is_unit_type, is_array_type, is_dyn_type, is_struct_type, is_enum_type, is_fn_trait_type } :: import("../../types/guards.yo");
 { type_is_control_bound } :: import("../../types/utils.yo");
 { Environment, get_variables_from_env, find_innermost_frame_with_given_variable, Variable } :: import("../../env.yo");
 { is_builtin_yo_inline_function } :: import("../constants.yo");
@@ -1488,27 +1488,73 @@ generate_other_function_call :: (fn(expr : AstExpr, indent : String, context : F
       cc
     },
     .None => {
-      ret_str := get_type_string(result_type, context.base);
-      cast := String.from("(");
-      cast.push_string(ret_str);
-      cast.push_str(" (*)(");
-      npt := param_types.len();
-      (pi : usize) = usize(0);
-      while(pi < npt, {
-        if(pi > usize(0), {
-          cast.push_str(", ");
+      // Dyn(Fn) VALUE call: the callee is a dyn fat pointer over the Fn trait
+      // (e.g. a `Dyn(Fn(...))` struct FIELD like SuspensionPointDetector.detect).
+      // Lower through the vtable: `(callee).vtable->call((callee).data, args...)`.
+      // Mirrors TS's `(h->apply).vtable->call((h->apply).data, 32)` emission.
+      // Without this the fallback below casts the fat-pointer STRUCT to a raw
+      // fn pointer ("operand of type X where arithmetic or pointer type is
+      // required") and drops the data argument.
+      (dyn_fn_callee : bool) = match(
+        context.base.get_expr_info(func_expr),
+        .Some(cei2) => match(
+          cei2.ty,
+          .DynT({ required_trait_types : dfr }) => {
+            (has_fn : bool) = false;
+            (dfi : usize) = usize(0);
+            while(dfi < dfr.len(), {
+              match(
+                dfr.get(dfi),
+                .Some(t) => if(is_fn_trait_type(t), {
+                  has_fn = true;
+                }),
+                .None => ()
+              );
+              dfi = (dfi + usize(1));
+            });
+            has_fn
+          },
+          _ => false
+        ),
+        .None => false
+      );
+      (arm_out : String) = String.new();
+      if(dyn_fn_callee, {
+        o2 := String.from("(");
+        o2.push_string(func_code.clone());
+        o2.push_str(").vtable->call((");
+        o2.push_string(func_code.clone());
+        o2.push_str(").data");
+        if(args_list.len() > usize(0), {
+          o2.push_str(", ");
+          o2.push_string(args_list.clone());
+        });
+        o2.push_str(")");
+        arm_out = o2;
+      }, {
+        ret_str := get_type_string(result_type, context.base);
+        cast := String.from("(");
+        cast.push_string(ret_str);
+        cast.push_str(" (*)(");
+        npt := param_types.len();
+        (pi : usize) = usize(0);
+        while(pi < npt, {
+          if(pi > usize(0), {
+            cast.push_str(", ");
+          });
+          cast.push_string(match(param_types.get(pi),.Some(t) => get_type_string(t, context.base),.None => String.from("void*")));
+          pi = (pi + usize(1));
         });
-        cast.push_string(match(param_types.get(pi),.Some(t) => get_type_string(t, context.base),.None => String.from("void*")));
-        pi = (pi + usize(1));
+        cast.push_str("))");
+        o := String.from("((");
+        o.push_string(cast);
+        o.push_string(func_code.clone());
+        o.push_str(")(");
+        o.push_string(args_list.clone());
+        o.push_str("))");
+        arm_out = o;
       });
-      cast.push_str("))");
-      o := String.from("((");
-      o.push_string(cast);
-      o.push_string(func_code.clone());
-      o.push_str(")(");
-      o.push_string(args_list.clone());
-      o.push_str("))");
-      o
+      arm_out
     }
   );
   // Effect unwind: a fn-pointer call through a `ctl`/handler param may set
diff --git a/yo-self/codegen/functions/dyn.yo b/yo-self/codegen/functions/dyn.yo
index 6f70a2cfd..9071debb6 100644
--- a/yo-self/codegen/functions/dyn.yo
+++ b/yo-self/codegen/functions/dyn.yo
@@ -18,7 +18,7 @@ open(import("std/string"));
 { get_func_type } :: import("../../function_value.yo");
 { is_boxed_type, is_fn_trait_type, is_function_type, is_void_type, is_pointer_type } :: import("../../types/guards.yo");
 { FunctionGenerationContext } :: import("./context.yo");
-{ type_key, get_type_string, sanitize_for_c_identifier } :: import("../utils/index.yo");
+{ type_key, get_type_string, sanitize_for_c_identifier, ImplClosureCallInfo } :: import("../utils/index.yo");
 { get_drop_function_for_type } :: import("../exprs/drop_dup.yo");
 /// func_id of a FuncVal EvalValue (mirrors collection.yo:_func_val_id).
 _func_val_id :: (fn(v : EvalValue) -> String)(
@@ -249,7 +249,38 @@ generate_dyn_wrapper_functions :: (fn(context : FunctionGenerationContext) -> un
                   em.emit_declaration_string_line(`static ${return_str} ${wrapper}(${params}) {`);
                   ncp := call_param_types.len();
                   is_void := is_void_type(call_result);
-                  if(is_boxed, {
+                  // Dyn'd CLOSURE: the boxed payload is the closure's CAPTURE
+                  // STRUCT — resolve the closure C fn via impl_closure_call_map
+                  // (capture-struct id → fn name) and call it directly with
+                  // `(void*)&box-><payload>` as the closure_context. Mirrors
+                  // TS's wrapper: `return closure_yoX((void*)&box->_u42_, arg1)`.
+                  cc_struct_id := match(
+                    impl_entry.concrete_type.clone(),
+                    .Struct({ id : csid }) => csid,
+                    _ => String.from("")
+                  );
+                  cc_info := if(cc_struct_id.len() > usize(0), context.base.impl_closure_call_map.get(cc_struct_id), Option(ImplClosureCallInfo).None);
+                  if(is_boxed && match(cc_info,.Some(_) => true,.None => false), {
+                    boxed_cname := match(context.base.get_type_c_name(type_key(data_type.clone())),.Some(cn) => cn,.None => `unknown_${type_key(data_type.clone())}`);
+                    field_name := _box_field_name(data_type.clone());
+                    em.emit_declaration_string_line(`  ${boxed_cname}* box = (${boxed_cname}*)self_ptr;`);
+                    cc_fn := match(cc_info,.Some(mp) => mp.function_c_name.clone(),.None => String.from(""));
+                    call := String.from("  ");
+                    if(!(is_void), {
+                      call.push_str("return ");
+                    });
+                    call.push_string(cc_fn);
+                    call.push_str("((void*)&box->");
+                    call.push_string(field_name.clone());
+                    (cai : usize) = usize(0);
+                    while(cai < ncp, {
+                      call.push_str(", arg");
+                      call.push_string((cai + usize(1)).to_string());
+                      cai = (cai + usize(1));
+                    });
+                    call.push_str(");");
+                    em.emit_declaration_string_line(call);
+                  }, if(is_boxed, {
                     boxed_cname := match(context.base.get_type_c_name(type_key(data_type.clone())),.Some(cn) => cn,.None => `unknown_${type_key(data_type.clone())}`);
                     field_name := _box_field_name(data_type.clone());
                     em.emit_declaration_string_line(`  ${boxed_cname}* box = (${boxed_cname}*)self_ptr;`);
@@ -284,7 +315,7 @@ generate_dyn_wrapper_functions :: (fn(context : FunctionGenerationContext) -> un
                       em.emit_declaration_string_line(`  ${return_str} zero = (${return_str})0;`);
                       em.emit_declaration_line("  return zero;");
                     });
-                  });
+                  }));
                   em.emit_declaration_line("}");
                   em.emit_declaration_line("");
                 },
diff --git a/yo-self/codegen/utils/index.yo b/yo-self/codegen/utils/index.yo
index e2682766a..2d2c992b3 100644
--- a/yo-self/codegen/utils/index.yo
+++ b/yo-self/codegen/utils/index.yo
@@ -803,8 +803,11 @@ get_type_string :: (fn(t : TypeValue, context : CodeGenContext) -> String)(
     // (mirrors TS getTypeString resolving through `resolvedConcreteType`).
     // Otherwise fall back to `void*` (an unresolved generic `Self`/SomeType is
     // passed by opaque pointer; field/method access casts at use).
-    .SomeT({ id : sid, name : snm }) => match(
-      lookup_some_resolved_concrete(sid),
+    .SomeT({ id : sid, name : snm, resolved_concrete : src }) => match(
+      // Prefer the PER-OBJECT resolved concrete (survives clone/substitute/
+      // intern — the id-keyed global misses when ids re-mint, e.g. the Box
+      // CTFE's cloned field SomeT in the Dyn(Fn) family). Global as fallback.
+      match(src,.Some(rc0) => Option(TypeValue).Some(rc0),.None => lookup_some_resolved_concrete(sid)),
       .Some(rct) => recur(rct, context),
       // An `extern("Yo", X : Type)` opaque type is a SomeT placeholder that
       // codegen emits as a concrete C typedef named X (e.g. `__yo_thread_t`,
diff --git a/yo-self/evaluator/values/dyn.yo b/yo-self/evaluator/values/dyn.yo
index b5f18b076..85dd31603 100644
--- a/yo-self/evaluator/values/dyn.yo
+++ b/yo-self/evaluator/values/dyn.yo
@@ -21,7 +21,7 @@ open(import("std/string"));
 { Environment, synthetic_token, get_variables_from_env, add_variable_to_env } :: import("../../env.yo");
 { EvalContext, ExpectedTypeCtx, ArgEntry, ArgValues, VarArgEntry } :: import("../context.yo");
 { EvalValue, is_func_val, create_type_value } :: import("../../value.yo");
-{ get_func_type } :: import("../../function_value.yo");
+{ get_func_type, get_closure_capture_info } :: import("../../function_value.yo");
 { create_specialized_function_inline } :: import("../calls/helper.yo");
 { TypeValue } :: import("../../types/definitions.yo");
 { t_unit, t_type, generate_some_type_id } :: import("../../types/creators.yo");
@@ -238,6 +238,23 @@ _resolve_dyn_trait_values :: (fn(dyn_type : TypeValue, value_type : TypeValue, c
               trait_vals.push(EvalValue.TraitVal(tname, field_names, field_vals));
               ()
             },
+            // Fn trait (Dyn(Fn(...)) — e.g. a dyn'd CLOSURE stored in a struct
+            // field): the closure ITSELF is the implementation; there is no
+            // registry impl to resolve. Push a marker TraitVal so
+            // `generate_dyn_call` proceeds (it bails on an EMPTY list) — the
+            // C wrapper for the synthetic `call` slot resolves the closure fn
+            // via the impl_closure_call_map (capture-struct id → closure C
+            // name), mirroring TS's implClosureCallMap.
+            .FnTraitT(_, _, _, _, _, _, _) => {
+              trait_vals.push(
+                EvalValue.TraitVal(
+                  String.from("Fn"),
+                  ArrayList(String).new(),
+                  ArrayList(Option(EvalValue)).new()
+                )
+              );
+              ()
+            },
             _ => ()
           ),
           .None => ()
@@ -363,10 +380,65 @@ evaluate_dyn_value :: (
     // get REAL ids (alloc_global_expr_id) — id=0 breaks the call's ExprInfo/forall
     // setup. The boxed expr is surfaced via runtime_arg_exprs_in_order (the field
     // generate_dyn_call reads). Trait evidence stays on the PRE-box concrete type.
+    //
+    // Dyn'd CLOSURE payload (Dyn(Fn(...)) struct fields): `Box(<fn type>)` is
+    // not constructible (fn-typed struct fields are rejected — the
+    // SuspensionPointDetector design note), and the closure's runtime value IS
+    // its capture struct — box THAT instead. Mirrors TS, whose box rtparam is
+    // the capture struct (`box_Impl(Fn...)(struct(base : i32))`); the capture
+    // struct is also the key the C wrapper resolves through
+    // impl_closure_call_map.
+    (ne_box_inner_ty : TypeValue) = ne_value_type;
+    match(
+      expr_info_table_get(ctx.expr_info_table, ast_expr_id(inner_ne)),
+      .Some(iiv) => match(
+        iiv.value,
+        .Some(ival) => match(
+          ival,
+          .FuncVal(__nfvd, _) => match(
+            get_closure_capture_info(__nfvd.*.func_id.clone()),
+            .Some(cci) => match(
+              inner_expected,
+              // Mirror TS: box with V = the Impl(Fn) SOMET whose
+              // resolved_concrete = the closure's CAPTURE STRUCT (TS's
+              // `box_Impl(Fn(...))` specialization; resolvedConcreteType
+              // carries the layout). The PER-OBJECT field survives the Box
+              // CTFE's clone/substitute/intern, so codegen's get_type_string
+              // lays the box payload out as the capture struct — the id-keyed
+              // global misses there (round-5 finding).
+              .Some(iec) => match(
+                iec.ty,
+                .SomeT(bx_id, bx_nm, bx_fl, bx_pt, bx_rtt, bx_rtl, bx_ntt, bx_ntl, bx_ier, bx_kft, _) => {
+                  ne_box_inner_ty = TypeValue.SomeT(
+                    bx_id,
+                    bx_nm,
+                    bx_fl,
+                    bx_pt,
+                    bx_rtt,
+                    bx_rtl,
+                    bx_ntt,
+                    bx_ntl,
+                    bx_ier,
+                    bx_kft,
+                    Option(TypeValue).Some(cci.capture_type)
+                  );
+                },
+                _ => ()
+              ),
+              .None => ()
+            ),
+            .None => ()
+          ),
+          _ => ()
+        ),
+        .None => ()
+      ),
+      .None => ()
+    );
     (ne_final_expr : AstExpr) = inner_ne;
     if(!(is_reference_struct_type(ne_value_type)) && !(is_dyn_type(ne_value_type)), {
       match(
-        _create_boxed_type(ne_value_type, env, ctx, exn),
+        _create_boxed_type(ne_box_inner_ty, env, ctx, exn),
         .Some(box_ty) => {
           ne_box_tok := synthetic_token(String.from("box"), env.module_path);
           ne_box_atom := AstExpr.Atom(alloc_global_expr_id(), ne_box_tok);
