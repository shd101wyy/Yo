open(import("std/fmt"));

Inner :: struct(v : i32);
derive(Inner, Clone);
Wrapper :: struct(inner : Box(Inner));
derive(Wrapper, Clone);

main :: (fn() -> unit)({
  w := Wrapper(inner : box(Inner(v : i32(7))));
  w2 := w.clone();
  println(`box-of-struct cloned`);
});

export(main);
