// @skip_wasm32-emscripten
// @skip_wasm32-wasi
open(import("std/fmt"));
open(import("std/error"));
{ Environment } :: import("../env/env.yo");
{ EvalValue, EvalResult } :: import("../evaluator/value.yo");
{ evaluate_module_body } :: import("../evaluator/eval.yo");
{ generate_exprs_from_code } :: import("../parser/parser.yo");
empty_env :: (fn() -> Environment)(Environment.new(`test`));

test("Phase 5ku: sum of prefix maxes of [1, 2, 6, 8, 5, 3, 7, 9, 4] = 59", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns := i32(0); mx := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); mx = cond((v > mx) => v, true => mx); s = (s + mx); i = (i + i32(1));\n});\npms := s;\nr := pms;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "59", "59"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5ku: sum of suffix mins of [1, 2, 6, 8, 5, 3, 7, 9, 4] = 27", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns2 := i32(0); mn := i32(9999); j := i32(8);\nwhile((j >= i32(0)), {\nv := arr.get(j).unwrap(); mn = cond((v < mn) => v, true => mn); s2 = (s2 + mn); j = (j - i32(1));\n});\nsms := s2;\nr := sms;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "27", "27"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5ku: count new maximums in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 5", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt := i32(0); mx2 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); is_new := cond((v > mx2) => i32(1), true => i32(0)); cnt = (cnt + is_new); mx2 = cond((v > mx2) => v, true => mx2); k = (k + i32(1));\n});\nr := cnt;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "5", "5"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5ku: sum of i*arr[i] in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 210", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns4 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); s4 = (s4 + (i * v)); i = (i + i32(1));\n});\nr := s4;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "210", "210"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kub: sum of 3-window max in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 55", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns5 := i32(0); j := i32(0);\nwhile((j < i32(7)), {\na := arr.get(j).unwrap(); b := arr.get((j + i32(1))).unwrap(); c := arr.get((j + i32(2))).unwrap();\nmx5 := cond((a > b) => a, true => b); mx5 = cond((c > mx5) => c, true => mx5); s5 = (s5 + mx5); j = (j + i32(1));\n});\nr := s5;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "55", "55"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kub: sum of 3-window min in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 21", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns6 := i32(0); k := i32(0);\nwhile((k < i32(7)), {\na := arr.get(k).unwrap(); b := arr.get((k + i32(1))).unwrap(); c := arr.get((k + i32(2))).unwrap();\nmn6 := cond((a < b) => a, true => b); mn6 = cond((c < mn6) => c, true => mn6); s6 = (s6 + mn6); k = (k + i32(1));\n});\nr := s6;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "21", "21"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kub: count 3-windows with sum>15 in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 5", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt7 := i32(0); j := i32(0);\nwhile((j < i32(7)), {\nws := (arr.get(j).unwrap() + arr.get((j + i32(1))).unwrap()); ws = (ws + arr.get((j + i32(2))).unwrap()); cnt7 = cond((ws > i32(15)) => (cnt7 + i32(1)), true => cnt7); j = (j + i32(1));\n});\nr := cnt7;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "5", "5"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kub: sum of middle elements in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 40", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns8 := i32(0); j := i32(1);\nwhile((j < i32(8)), {\ns8 = (s8 + arr.get(j).unwrap()); j = (j + i32(1));\n});\nr := s8;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "40", "40"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kuc: sum of elements > 5 in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 30", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns9 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); s9 = cond((v > i32(5)) => (s9 + v), true => s9); k = (k + i32(1));\n});\nr := s9;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "30", "30"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kuc: count i where arr[i]+arr[8-i]>10 in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 6", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt10 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nps := (arr.get(i).unwrap() + arr.get((i32(8) - i)).unwrap()); cnt10 = cond((ps > i32(10)) => (cnt10 + i32(1)), true => cnt10); i = (i + i32(1));\n});\nr := cnt10;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "6", "6"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kuc: sum of |arr[i]-5| in [1, 2, 6, 8, 5, 3, 7, 9, 4] = 20", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns11 := i32(0); j := i32(0);\nwhile((j < i32(9)), {\nv := arr.get(j).unwrap(); d := cond((v > i32(5)) => (v - i32(5)), true => (i32(5) - v)); s11 = (s11 + d); j = (j + i32(1));\n});\nr := s11;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "20", "20"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kuc: count multiples of 6 in 1..20 = 3", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt12 := i32(0); n := i32(1);\nwhile((n <= i32(20)), {\ncnt12 = cond(((n % i32(6)) == i32(0)) => (cnt12 + i32(1)), true => cnt12); n = (n + i32(1));\n});\nr := cnt12;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "3", "3"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kud: prefix_max_sum + suffix_min_sum + new_max_cnt = 59+27+5 = 91", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns := i32(0); mx := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); mx = cond((v > mx) => v, true => mx); s = (s + mx); i = (i + i32(1));\n});\npms := s;\ns2 := i32(0); mn := i32(9999); j := i32(8);\nwhile((j >= i32(0)), {\nv := arr.get(j).unwrap(); mn = cond((v < mn) => v, true => mn); s2 = (s2 + mn); j = (j - i32(1));\n});\nsms := s2;\ncnt := i32(0); mx2 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); is_new := cond((v > mx2) => i32(1), true => i32(0)); cnt = (cnt + is_new); mx2 = cond((v > mx2) => v, true => mx2); k = (k + i32(1));\n});\nr := ((pms + sms) + cnt);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "91", "91"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kud: sum_iv + win_max_sum + win_min_sum = 210+55+21 = 286", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns4 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); s4 = (s4 + (i * v)); i = (i + i32(1));\n});\ns5 := i32(0); j := i32(0);\nwhile((j < i32(7)), {\na := arr.get(j).unwrap(); b := arr.get((j + i32(1))).unwrap(); c := arr.get((j + i32(2))).unwrap();\nmx5 := cond((a > b) => a, true => b); mx5 = cond((c > mx5) => c, true => mx5); s5 = (s5 + mx5); j = (j + i32(1));\n});\ns6 := i32(0); k := i32(0);\nwhile((k < i32(7)), {\na := arr.get(k).unwrap(); b := arr.get((k + i32(1))).unwrap(); c := arr.get((k + i32(2))).unwrap();\nmn6 := cond((a < b) => a, true => b); mn6 = cond((c < mn6) => c, true => mn6); s6 = (s6 + mn6); k = (k + i32(1));\n});\nr := ((s4 + s5) + s6);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "286", "286"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kud: win_cnt_gt15 + win_mid_sum + sum_gt5 = 5+40+30 = 75", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt7 := i32(0); j := i32(0);\nwhile((j < i32(7)), {\nws := (arr.get(j).unwrap() + arr.get((j + i32(1))).unwrap()); ws = (ws + arr.get((j + i32(2))).unwrap()); cnt7 = cond((ws > i32(15)) => (cnt7 + i32(1)), true => cnt7); j = (j + i32(1));\n});\ns8 := i32(0); j := i32(1);\nwhile((j < i32(8)), {\ns8 = (s8 + arr.get(j).unwrap()); j = (j + i32(1));\n});\ns9 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); s9 = cond((v > i32(5)) => (s9 + v), true => s9); k = (k + i32(1));\n});\nr := ((cnt7 + s8) + s9);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "75", "75"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kud: cnt_sym_gt10 + sum_abs_dev + cnt_mul6 = 6+20+3 = 29", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt10 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nps := (arr.get(i).unwrap() + arr.get((i32(8) - i)).unwrap()); cnt10 = cond((ps > i32(10)) => (cnt10 + i32(1)), true => cnt10); i = (i + i32(1));\n});\ns11 := i32(0); j := i32(0);\nwhile((j < i32(9)), {\nv := arr.get(j).unwrap(); d := cond((v > i32(5)) => (v - i32(5)), true => (i32(5) - v)); s11 = (s11 + d); j = (j + i32(1));\n});\ncnt12 := i32(0); n := i32(1);\nwhile((n <= i32(20)), {\ncnt12 = cond(((n % i32(6)) == i32(0)) => (cnt12 + i32(1)), true => cnt12); n = (n + i32(1));\n});\nr := ((cnt10 + s11) + cnt12);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "29", "29"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kue: prefix_max_sum + win_max_sum + sum_gt5 = 59+55+30 = 144", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns := i32(0); mx := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); mx = cond((v > mx) => v, true => mx); s = (s + mx); i = (i + i32(1));\n});\npms := s;\ns5 := i32(0); j := i32(0);\nwhile((j < i32(7)), {\na := arr.get(j).unwrap(); b := arr.get((j + i32(1))).unwrap(); c := arr.get((j + i32(2))).unwrap();\nmx5 := cond((a > b) => a, true => b); mx5 = cond((c > mx5) => c, true => mx5); s5 = (s5 + mx5); j = (j + i32(1));\n});\ns9 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); s9 = cond((v > i32(5)) => (s9 + v), true => s9); k = (k + i32(1));\n});\nr := ((pms + s5) + s9);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "144", "144"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kue: suffix_min_sum + win_min_sum + cnt_sym_gt10 = 27+21+6 = 54", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns2 := i32(0); mn := i32(9999); j := i32(8);\nwhile((j >= i32(0)), {\nv := arr.get(j).unwrap(); mn = cond((v < mn) => v, true => mn); s2 = (s2 + mn); j = (j - i32(1));\n});\nsms := s2;\ns6 := i32(0); k := i32(0);\nwhile((k < i32(7)), {\na := arr.get(k).unwrap(); b := arr.get((k + i32(1))).unwrap(); c := arr.get((k + i32(2))).unwrap();\nmn6 := cond((a < b) => a, true => b); mn6 = cond((c < mn6) => c, true => mn6); s6 = (s6 + mn6); k = (k + i32(1));\n});\ncnt10 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nps := (arr.get(i).unwrap() + arr.get((i32(8) - i)).unwrap()); cnt10 = cond((ps > i32(10)) => (cnt10 + i32(1)), true => cnt10); i = (i + i32(1));\n});\nr := ((sms + s6) + cnt10);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "54", "54"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kue: new_max_cnt + win_cnt_gt15 + sum_abs_dev = 5+5+20 = 30", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ncnt := i32(0); mx2 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); is_new := cond((v > mx2) => i32(1), true => i32(0)); cnt = (cnt + is_new); mx2 = cond((v > mx2) => v, true => mx2); k = (k + i32(1));\n});\ncnt7 := i32(0); j := i32(0);\nwhile((j < i32(7)), {\nws := (arr.get(j).unwrap() + arr.get((j + i32(1))).unwrap()); ws = (ws + arr.get((j + i32(2))).unwrap()); cnt7 = cond((ws > i32(15)) => (cnt7 + i32(1)), true => cnt7); j = (j + i32(1));\n});\ns11 := i32(0); j := i32(0);\nwhile((j < i32(9)), {\nv := arr.get(j).unwrap(); d := cond((v > i32(5)) => (v - i32(5)), true => (i32(5) - v)); s11 = (s11 + d); j = (j + i32(1));\n});\nr := ((cnt + cnt7) + s11);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "30", "30"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5kue: sum_iv + win_mid_sum + cnt_mul6 = 210+40+3 = 253", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(5), i32(3), i32(7), i32(9), i32(4)];\ns4 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); s4 = (s4 + (i * v)); i = (i + i32(1));\n});\ns8 := i32(0); j := i32(1);\nwhile((j < i32(8)), {\ns8 = (s8 + arr.get(j).unwrap()); j = (j + i32(1));\n});\ncnt12 := i32(0); n := i32(1);\nwhile((n <= i32(20)), {\ncnt12 = cond(((n % i32(6)) == i32(0)) => (cnt12 + i32(1)), true => cnt12); n = (n + i32(1));\n});\nr := ((s4 + s8) + cnt12);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "253", "253"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});
