// @skip_wasm32-emscripten
// @skip_wasm32-wasi
open(import("std/fmt"));
open(import("std/error"));
{ Environment } :: import("../env/env.yo");
{ EvalValue, EvalResult } :: import("../evaluator/value.yo");
{ evaluate_module_body } :: import("../evaluator/eval.yo");
{ generate_exprs_from_code } :: import("../parser/parser.yo");
empty_env :: (fn() -> Environment)(Environment.new(`test`));

test("Phase 5lx: sum of prefix maxes of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 58", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ns := i32(0); mx := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); mx = cond((v > mx) => v, true => mx); s = (s + mx); i = (i + i32(1));\n});\npms := s;\nr := pms;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "58", "58"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: sum of suffix mins of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 30", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ns2 := i32(0); mn := i32(9999); j := i32(8);\nwhile((j >= i32(0)), {\nv := arr.get(j).unwrap(); mn = cond((v < mn) => v, true => mn); s2 = (s2 + mn); j = (j - i32(1));\n});\nsms := s2;\nr := sms;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "30", "30"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: count new maximums of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 5", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ncnt := i32(0); mx2 := i32(0); k := i32(0);\nwhile((k < i32(9)), {\nv := arr.get(k).unwrap(); is_new := cond((v > mx2) => i32(1), true => i32(0)); cnt = (cnt + is_new); mx2 = cond((v > mx2) => v, true => mx2); k = (k + i32(1));\n});\nr := cnt;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "5", "5"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: 0-based weighted sum of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 209", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ns4 := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); s4 = (s4 + (i * v)); i = (i + i32(1));\n});\nr := s4;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "209", "209"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: sum of even-indexed elements of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 28", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nse := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); se = (se + v); i = (i + i32(2));\n});\nr := se;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "28", "28"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: sum of odd-indexed elements of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 17", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nso := i32(0); i := i32(1);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); so = (so + v); i = (i + i32(2));\n});\nr := so;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "17", "17"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: count elements greater than 5 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 4", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ncnt := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); cnt = cond((v > i32(5)) => (cnt + i32(1)), true => cnt); i = (i + i32(1));\n});\nr := cnt;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "4", "4"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: count elements less than 5 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 4", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ncnt := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); cnt = cond((v < i32(5)) => (cnt + i32(1)), true => cnt); i = (i + i32(1));\n});\nr := cnt;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "4", "4"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: count elements equal to 5 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 1", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ncnt := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); cnt = cond((v == i32(5)) => (cnt + i32(1)), true => cnt); i = (i + i32(1));\n});\nr := cnt;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "1", "1"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: count mirror-pairs summing over 10 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 5", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\ncnt := i32(0); i := i32(0);\nwhile((i < i32(9)), {\na := arr.get(i).unwrap(); b := arr.get(usize((i32(8) - i))).unwrap(); cnt = cond(((a + b) > i32(10)) => (cnt + i32(1)), true => cnt); i = (i + i32(1));\n});\nr := cnt;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "5", "5"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: maximum element of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 9", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nmx := i32(0); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); mx = cond((v > mx) => v, true => mx); i = (i + i32(1));\n});\nr := mx;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "9", "9"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: minimum element of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 1", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nmn := i32(9999); i := i32(0);\nwhile((i < i32(9)), {\nv := arr.get(i).unwrap(); mn = cond((v < mn) => v, true => mn); i = (i + i32(1));\n});\nr := mn;\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "1", "1"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T1+T2+T3 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 93", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT1 := i32(0); mx1 := i32(0); i := i32(0);\nwhile((i < i32(9)), { v := arr.get(i).unwrap(); mx1 = cond((v > mx1) => v, true => mx1); T1 = (T1 + mx1); i = (i + i32(1)); });\nT2 := i32(0); mn2 := i32(9999); j := i32(8);\nwhile((j >= i32(0)), { v := arr.get(j).unwrap(); mn2 = cond((v < mn2) => v, true => mn2); T2 = (T2 + mn2); j = (j - i32(1)); });\nT3 := i32(0); mx3 := i32(0); k := i32(0);\nwhile((k < i32(9)), { v := arr.get(k).unwrap(); is_new := cond((v > mx3) => i32(1), true => i32(0)); T3 = (T3 + is_new); mx3 = cond((v > mx3) => v, true => mx3); k = (k + i32(1)); });\nr := ((T1 + T2) + T3);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "93", "93"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T4+T5+T6 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 254", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT4 := i32(0); i4 := i32(0);\nwhile((i4 < i32(9)), { v := arr.get(i4).unwrap(); T4 = (T4 + (i4 * v)); i4 = (i4 + i32(1)); });\nT5 := i32(0); i5 := i32(0);\nwhile((i5 < i32(9)), { v := arr.get(i5).unwrap(); T5 = (T5 + v); i5 = (i5 + i32(2)); });\nT6 := i32(0); i6 := i32(1);\nwhile((i6 < i32(9)), { v := arr.get(i6).unwrap(); T6 = (T6 + v); i6 = (i6 + i32(2)); });\nr := ((T4 + T5) + T6);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "254", "254"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T7+T8+T9 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 9", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT7 := i32(0); T8 := i32(0); T9 := i32(0); i := i32(0);\nwhile((i < i32(9)), { v := arr.get(i).unwrap(); T7 = cond((v > i32(5)) => (T7 + i32(1)), true => T7); T8 = cond((v < i32(5)) => (T8 + i32(1)), true => T8); T9 = cond((v == i32(5)) => (T9 + i32(1)), true => T9); i = (i + i32(1)); });\nr := ((T7 + T8) + T9);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "9", "9"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T10+T11+T12 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 15", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT10 := i32(0); i10 := i32(0);\nwhile((i10 < i32(9)), { a2 := arr.get(i10).unwrap(); b2 := arr.get(usize((i32(8) - i10))).unwrap(); T10 = cond(((a2 + b2) > i32(10)) => (T10 + i32(1)), true => T10); i10 = (i10 + i32(1)); });\nT11 := i32(0); T12 := i32(9999); im := i32(0);\nwhile((im < i32(9)), { v := arr.get(im).unwrap(); T11 = cond((v > T11) => v, true => T11); T12 = cond((v < T12) => v, true => T12); im = (im + i32(1)); });\nr := ((T10 + T11) + T12);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "15", "15"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T1+T5+T9 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 87", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT1 := i32(0); mx1 := i32(0); i := i32(0);\nwhile((i < i32(9)), { v := arr.get(i).unwrap(); mx1 = cond((v > mx1) => v, true => mx1); T1 = (T1 + mx1); i = (i + i32(1)); });\nT5 := i32(0); i5 := i32(0);\nwhile((i5 < i32(9)), { v := arr.get(i5).unwrap(); T5 = (T5 + v); i5 = (i5 + i32(2)); });\nT9 := i32(0); i9 := i32(0);\nwhile((i9 < i32(9)), { v := arr.get(i9).unwrap(); T9 = cond((v == i32(5)) => (T9 + i32(1)), true => T9); i9 = (i9 + i32(1)); });\nr := ((T1 + T5) + T9);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "87", "87"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T2+T6+T10 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 52", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT2 := i32(0); mn2 := i32(9999); j := i32(8);\nwhile((j >= i32(0)), { v := arr.get(j).unwrap(); mn2 = cond((v < mn2) => v, true => mn2); T2 = (T2 + mn2); j = (j - i32(1)); });\nT6 := i32(0); i6 := i32(1);\nwhile((i6 < i32(9)), { v := arr.get(i6).unwrap(); T6 = (T6 + v); i6 = (i6 + i32(2)); });\nT10 := i32(0); i10 := i32(0);\nwhile((i10 < i32(9)), { a2 := arr.get(i10).unwrap(); b2 := arr.get(usize((i32(8) - i10))).unwrap(); T10 = cond(((a2 + b2) > i32(10)) => (T10 + i32(1)), true => T10); i10 = (i10 + i32(1)); });\nr := ((T2 + T6) + T10);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "52", "52"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T3+T7+T11 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 18", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT3 := i32(0); mx3 := i32(0); k := i32(0);\nwhile((k < i32(9)), { v := arr.get(k).unwrap(); is_new := cond((v > mx3) => i32(1), true => i32(0)); T3 = (T3 + is_new); mx3 = cond((v > mx3) => v, true => mx3); k = (k + i32(1)); });\nT7 := i32(0); i7 := i32(0);\nwhile((i7 < i32(9)), { v := arr.get(i7).unwrap(); T7 = cond((v > i32(5)) => (T7 + i32(1)), true => T7); i7 = (i7 + i32(1)); });\nT11 := i32(0); i11 := i32(0);\nwhile((i11 < i32(9)), { v := arr.get(i11).unwrap(); T11 = cond((v > T11) => v, true => T11); i11 = (i11 + i32(1)); });\nr := ((T3 + T7) + T11);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "18", "18"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});

test("Phase 5lx: T4+T8+T12 of [1, 2, 6, 8, 7, 4, 5, 3, 9] = 214", {
  env := empty_env();
  given(exn) := Exception(
    throw : (
      (err) -> {
        assert(false, "e");
        escape(());
      }
    )
  );
  src := "arr := [i32(1), i32(2), i32(6), i32(8), i32(7), i32(4), i32(5), i32(3), i32(9)];\nT4 := i32(0); i4 := i32(0);\nwhile((i4 < i32(9)), { v := arr.get(i4).unwrap(); T4 = (T4 + (i4 * v)); i4 = (i4 + i32(1)); });\nT8 := i32(0); T12 := i32(9999); im := i32(0);\nwhile((im < i32(9)), { v := arr.get(im).unwrap(); T8 = cond((v < i32(5)) => (T8 + i32(1)), true => T8); T12 = cond((v < T12) => v, true => T12); im = (im + i32(1)); });\nr := ((T4 + T8) + T12);\nexport(r);\n";
  exprs := generate_exprs_from_code(src, using(exn));
  result := evaluate_module_body(exprs, &(env));
  match(
    result,
    .None => assert(false, "N"),
    .Some(m) => match(
      m,
      .ModuleVal(_, v) => {
        match(
          v.get(usize(0)),
          .Some(x) => match(x,.IntLit(n) => assert(n.as_str() == "214", "214"), _ => assert(false, "t")),
          .None => assert(false, "v")
        );
      },
      _ => assert(false, "m")
    )
  );
});
