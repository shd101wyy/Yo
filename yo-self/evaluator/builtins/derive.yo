//! evaluator/builtins/derive — stub for evaluate_derive.
//!
//! Phase 3 stub. Full implementation planned for Phase 3.
//! 1:1 port of `src/evaluator/builtins/derive.ts`.

open import "std/string";
{ AstExpr, ast_expr_token, make_err_expr } :: import "../../expr/expr.yo";
{ Environment }                             :: import "../../env/env.yo";
{ EvalContext }                             :: import "../context.yo";
{ format_error_message }                    :: import "../../error/error.yo";
{ Exception }                               :: import "std/error";

evaluate_derive :: (fn(
  expr : AstExpr,
  env  : Environment,
  ctx  : *(EvalContext),
  using(exn : Exception)
) -> AstExpr)({
  _ := env;
  _ := ctx;
  exn.throw(dyn format_error_message(
    ast_expr_token(expr),
    String.from("evaluate_derive: not yet implemented (Phase 3)"),
    false,
    .None
  ));
  make_err_expr()
});

export evaluate_derive;
