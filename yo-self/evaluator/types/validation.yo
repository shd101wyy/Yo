//! evaluator/types/validation — dispose-function signature validation.
//!
//! TS counterpart: `src/evaluator/types/validation.ts` (~43 lines).
//!
//! Status: stub. The only export in the TS file is
//! `validateDisposeFunction(moduleElement, token)`, which checks that a
//! `dispose : fn(self : Self) -> unit` field matches the expected
//! signature. Nothing in the production TS evaluator currently calls it
//! (the dispose-signature check is enforced elsewhere).
//!
//! No yo-self code references this module yet; this stub exists so the
//! evaluator/types/ directory mirrors the TS tree 1-to-1. Port the real
//! `validateDisposeFunction` body when a yo-self caller appears, mirroring
//! the TS implementation.
pragma(Pragma.AllowUnsafe);
